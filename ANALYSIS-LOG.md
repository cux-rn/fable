# Fable 分析日志

## 2026-09-13 — 参考实现与初步测试（手机端沙盒）

**实现**：`fable.py`（Fable-P / AEAD / Stream / Hash-XOF），`test_fable.py`，`test_vectors_v0.1.json`。

**功能测试**：AEAD 各长度往返、密文/标签/AD/nonce 篡改检测、Stream 截断与块重排检测 —— 全部通过。

**雪崩测试**（300 组随机状态，全部 512×512 输入-输出比特对）：

| 轮数 | 平均翻转率 | 最大偏差 | 偏差<0.1 比例 |
|---|---|---|---|
| 1 | 0.356 | 0.500 | 44% |
| 2 | 0.500 | 0.153 | 100% |
| 3 | 0.500 | 0.137 | 100% |
| 4 | 0.500 | 0.157 | 100% |

结论：2 轮达到完整雪崩。2～4 轮的最大偏差 ≈0.15 与 300 次采样的统计噪声（4.5σ≈0.13）一致，未见结构性偏差。需在电脑上用 ≥10^5 次采样复测以检测 <0.02 级别的偏差。

**旋转常数粗筛**（150 组随机旋转量，1 轮雪崩均值）：
基线 (16,12,8,7) 排第 42/150，得分 0.357；最高 0.378。
结论：单轮扩散速度不能作为唯一标准，ChaCha 常数是按差分概率而非扩散速度选出的。§7.3 的 2、3 条（差分特征概率界）才是决定性指标，需 SAT/MILP，转电脑完成。**旋转常数维持临时状态。**

## 待办（2026-09-14 v0.3 阶段后更新）
- v0.3 已完成：参数集切换 + 双集测试向量；默认集吞吐（P_8 2.00 GB/s ≈ ChaCha20-Poly1305）、AVX2 解密、4/8 线程扩展；P_8 伪造拆分界 ≤ 2^-132（2 轮速率输入 ≥ 39 + 3 × 2 轮无约束 ≥ 31）；线性 hull（1 轮无 hull，2 轮 ≈ ×9）；`docs/key-commitment.md`；`docs/security-bounds.md`。
- 仍未完成：2 轮线性最优精确值（≥ 19）；3 轮速率受限直接界（≥ 43，对拆分无改善）；NEON；规范 §4.1 应注明随机 N_f 的碰撞项 F²/2^153 与计数器式 N_f 选项；§7.2 速率 320 需改为单用户限量变体。

### （历史）待办（2026-09-13 v0.2 阶段后）
1. ~~C 实现 + 10^5 级雪崩复测~~（完成，v0.1 阶段 1、2）
2. ~~Q 函数与 1～2 轮差分特征概率~~（完成；单 Q = 2^0、1 轮 = 2^-2、2 轮 ≥ 31 已终止）。~~速率受限差分~~（完成，v0.2 阶段 1：1 轮 = 2^-22，内部碰撞半轮不可能）。**未完成**：3 轮速率受限直接界、"任意输入 + 容量输出为零"的 1 轮界（用于收紧 P_6 伪造界 2^-86 → 2^-128）
3. ~~slide、固定点~~、~~旋转差分~~（完成）。~~线性 1 轮精确~~（完成，2^-1）。**未完成**：线性 hull、2 轮以上线性直接界
4. ~~差分-线性可达轮数评估~~（完成，v0.2 阶段 2：P_6 余量 1.6×，P_12 3.2×）。**建议**：规范 §0 写明 P_6 ≡ ChaCha12 余量，删除 §7.2 的 5 轮方向
5. 密钥承诺归约书面化；多用户与 STREAM 组合证明检查
6. ~~Fable-Stream 8 路 AVX2~~（完成，1.97 GB/s）。**未完成**：NEON 版本、多线程吞吐、解密端 AVX2

---

## 2026-09-13 — 电脑端阶段 1：C 实现 Fable-P / AEAD

**环境**：Windows 11，clang 21.1.0（x86_64-pc-windows-msvc），Python 3.10.9，python-sat 1.9（CaDiCaL 1.5.3），CPU Intel Raptor Lake（24 线程，AVX2）。

**文件**：
- `c/fable_p.h` / `c/fable_p.c` —— 置换。`fable_permute(s, rounds)` 与 `fable.py` 的 `permute` 同语义（r 轮使用 RC[12-r..11]）；另提供 `fable_permute_rot`（自定义旋转量，供筛选）和 `fable_permute_noconst`（轮常量置零，供对称性检查）。
- `c/fable_aead.h` / `c/fable_aead.c` —— AEAD 加/解密与 XOF 的纯 C 移植。
- `c/build.sh` —— 用 clang 构建 `build/fable.dll`。
- `tests/check_c_vectors.py` —— ctypes 加载 DLL，比对测试向量并与 Python 参考交叉校验。

**复现**：
```
bash c/build.sh && python tests/check_c_vectors.py
```

**结果**：`permute12_zero`、5 组 AEAD 向量（加密、解密、篡改检测）、2 组 hash256 向量全部一致；1～12 轮每种轮数各 50 组随机状态、200 组随机旋转量的置换输出与 Python 逐位一致；36 组随机长度组合的 AEAD 密文与 Python 一致。`RESULT: ALL OK`。

---

## 2026-09-13 — 电脑端阶段 2：雪崩复测（C 版，5×10^5 组/轮数）

**工具**：`c/avalanche.c`（xoshiro256** 随机状态，翻转全部 512 输入比特，统计 512×512 对翻转计数），`tools/avalanche_report.py`（汇总）。原始数据：`data/avalanche/av_r{1..6}.u32`（512×512 小端 uint32 计数，行=输入比特，列=输出比特）、`av_r*.txt`（各进程原始输出）、`summary.md`（汇总表）。

**复现**：
```
clang -O3 -std=c11 c/avalanche.c c/fable_p.c -o build/avalanche.exe
for r in 1 2 3 4 5 6; do ./build/avalanche.exe $r 500000 $((1000+r)) data/avalanche/av_r$r.u32 > data/avalanche/av_r$r.txt; done
python tools/avalanche_report.py
```

**结果**（噪声 σ = 0.5/√500000 = 0.00071；262144 对独立高斯极值的期望 ≈ 4.9σ = 0.0035）：

| 轮数 | 平均翻转率 | 最大偏差 | 偏差>0.02 对数 | 偏差>0.01 对数 | 偏差>0.005 对数 | 零翻转对数 |
|---|---|---|---|---|---|---|
| 1 | 0.35588 | 0.50000 | 188107 | 199886 | 206640 | 1881 |
| 2 | 0.50000 | 0.00320 | 0 | 0 | 0 | 0 |
| 3 | 0.50000 | 0.00299 | 0 | 0 | 0 | 0 |
| 4 | 0.50000 | 0.00320 | 0 | 0 | 0 | 0 |
| 5 | 0.50000 | 0.00327 | 0 | 0 | 0 | 0 |
| 6 | 0.50000 | 0.00356 | 0 | 0 | 0 | 0 |

**判断**：2～6 轮的最大偏差 0.0030～0.0036 恰为纯噪声下 26 万对样本的期望极值，且偏差直方图全部落在 [0, 0.005) 第一个 bin；**不存在 0.02 级别的结构性偏差，甚至 0.005 级别也不存在**。手机端 300 样本测得的 0.15 最大偏差完全是统计噪声。1 轮时有 1881 对（0.7%）输入比特永远不影响输出比特、约 57% 的对是确定性（翻转率 0 或 1），符合"1 轮字级完整扩散、比特级未达雪崩"的预期。
注意：该测试对 (16,12,8,7) 的雪崩指标 §7.3-1 在 2 轮已饱和（偏差被噪声淹没），因此 §7.3-1 在 2 轮无法区分旋转常数，只能在 1 轮或以更细指标区分。

---

## 2026-09-13 — 电脑端阶段 3：差分分析（SAT）与旋转常数筛选

**环境与模型**：python-sat 1.9（CaDiCaL 1.5.3 用于无超时精确求解，Glucose 4 用于需中断的限时求解）。模型 `sat/fable_diff.py`：模加的 XOR 差分用 Lipmaa–Moriai 精确条件建模（每个加法 31 个权重变量，权重 w ⇔ 概率 2^-w），XOR 为线性、旋转为连线置换、轮常量对差分透明；总权重用 ITotalizer 递增约束，权重 W 的最优性 = "≤W−1 UNSAT 且 ≤W SAT"。所有结果都是**差分特征（trail）概率**上界（独立轮假设），差分（differential）概率只会更高。搜索到的轨迹用 `sat/verify_trail.py` 经验抽样验证。CryptoSMT 未用（依赖 STP，Windows 下不便），python-sat 实现的是同一类模型。

**复现**：
```
python sat/fable_diff.py q --rot 16,12,8,7                      # (a) 单 Q
python sat/fable_diff.py perm --rounds 1 --rot 16,12,8,7        # (b) 1 轮，0.1 s
python sat/fable_diff.py perm --rounds 2 --rot 16,12,8,7 --solver cadical153   # (b) 2 轮，小时级
python sat/verify_trail.py data/diff/perm_r1_rot16_12_8_7.json
python sat/rot_screen_sat.py --metric q --all --procs 2 --out data/diff/screen_q_all.csv              # (c) 单 Q 全空间
python sat/rot_screen_sat.py --metric r1 --all --procs 6 --kmax 10 --out data/diff/screen_r1_all.csv  # (c) 1 轮（跑了 254576 组后停止）
python sat/rot_screen_sat.py --metric r1c --from data/diff/cands_r1c.txt --thresholds 6,9,11 --kmax 16 --procs 8 --out data/diff/screen_r1c_cands.csv   # 1.5 轮（跑到 2000 组停止）
python tools/av1_tiebreak.py --from data/diff/cands_top12.txt --trials 4000 --out data/diff/av1_tiebreak_top12.csv
python sat/rot_screen_sat.py --metric r2 --from data/diff/cands_r2.txt --thresholds 16,20 --noexact --procs 8 --out data/diff/screen_r2_top.csv
python sat/screen_report.py <csv>
```
原始数据：`data/diff/*.json`（含轨迹、每次求解日志）、`screen_q_all.csv.gz`、`screen_r1_all.csv.gz`、`screen_r1c_cands.csv`、`av1_tiebreak_top12.csv`、`screen_r2_top.csv`、`perm_r2_baseline_cadical.log`。

### (a) 单 Q 函数，旋转 (16,12,8,7)

**最优差分特征权重 0（概率 1）**。轨迹：Δ(a,b,c,d) = (0, 0x80000000, 0, 0x80008000) → (0x80000000, 0x00000040, 0x80000000, 0)。机理：Δb=MSB 经 a+=b 无进位地进入 Δa；Δd 里预先放的 bit15 在 d⊕=a 后剩下 bit15，<<<16 后变 MSB，再无进位进入 c；b⊕=c 抵消为 0……全程只有最高位差分在走，四次模加都不付代价。经验验证 10^5 组：100% 命中。
**这个构造对任何 (r0,r1,r2,r3) 都存在**（只需 Δd = MSB ⊕ bit(31−r0)），全空间 32^4 = 1,048,576 组 SAT 逐一验证：**全部权重 0**（`screen_q_all.csv.gz`）。结论：**规范 §7.3 第 2 条"1 轮 Q 的最优差分特征概率"对本结构完全没有区分度，必须更换。** 这也是 ChaCha QR 的已知性质（单 QR 存在概率 1 差分），Fable 直接继承。

### (b) Fable-P 1～4 轮最优差分特征概率

| 轮数 | 已证下界（权重） | 找到的轨迹权重 | 概率上界 | 状态 | 耗时 |
|---|---|---|---|---|---|
| 1 | 2 | 2 | 2^-2 | **最优（精确）** | 0.1 s |
| 2 | ≥ 31（CaDiCaL 仍在跑，见 `perm_r2_baseline_cadical.log`） | 未找到 | ≤ 2^-31 | 下界，未收敛 | k=28 UNSAT 6041 s，k=29 8039 s，k=30 7657 s |
| 3 | ≥ 33（= W1 + W2，轨迹可拆分为 1 轮 + 2 轮独立段） | — | ≤ 2^-33 | 平凡拆分界 | — |
| 4 | ≥ 62（= 2·W2） | — | ≤ 2^-62 | 平凡拆分界 | — |

1 轮最优轨迹（`perm_r1_rot16_12_8_7.json`）：输入差分集中在第 2、3 列（0x80000000/0x80800000/0x80008000/0x80 等 MSB 派生位），列步后只剩两个 MSB，对角步付 2 权重；经验验证 2×10^5 组，命中率 0.2498 ≈ 2^-2。
2 轮直接求解在 k≈20 以后每级耗时按 1.5～2 倍增长（k=20 322 s，k=24 1359 s，k=28 6041 s），单线程下 3～4 轮的精确最优不可行；3、4 轮只给出拆分下界。要得到 §7.3 第 3 条要求的"4 轮内无 > 2^-128 路径"，需要 Matsui 式分段界或 MILP 分层求解，留待下一阶段（建议：先精确求 2 轮，再用 2+2 拆分得 4 轮 ≥ 2·W2；若 W2 ≥ 32 即得 4 轮 ≤ 2^-64，6 轮 ≤ 2^-96，仍达不到 2^-128，P_6 的安全余量要靠差分不是特征的论证或更多轮）。

### (c) 旋转常数筛选

**筛选指标逐级退化的情况**（这是本阶段最重要的发现）：

| 指标 | 覆盖范围 | 结果 |
|---|---|---|
| 单 Q 最优权重（§7.3-2） | 全部 32^4 | 100% 为 0，无区分度 |
| 1 轮 Fable-P 最优权重 | 254,576 组（含全部 r0∈{0..7} 前缀） | 不含旋转 0 的 201,422 组 **100% 为 2**；含旋转 0 的为 0 或 1。无区分度，中止 |
| 1.5 轮（1 轮 + 列步）最优权重 | 3001 组候选中前 2000 组（基线、(20,12,8,7)、630 组 SIMD 友好 r0,r2∈{8,16,24}、随机 1368 组） | 分布：6:1, 7:5, 8:73, 9:62, 10:318, 11:203, **12:1338**；无一超过 12。基线 = 12，与 67% 的组合并列最高类 |
| 1 轮雪崩平均翻转率（§7.3-1，2 轮已饱和故取 1 轮） | 上述 1338 组 | 作为并列打破：基线 0.3558，排第 524/1338；最高 0.3814 |

12 看起来是 1.5 轮的结构上限（2 权重的 1 轮 MSB 轨迹 + 列步再付 10），旋转量只决定能否更差、不能更好。因此"取最好的 20 组"是按 1.5 轮 = 12 类内的 1 轮雪崩排序取前 20（`cands_r2.txt`，其中 8 组 SIMD 友好），再加基线共 21 组做 2 轮阈值检查。

**2 轮排名表**（21 组；阈值级联 "≤16? ≤20? ≤24?" 全部 UNSAT，即每组 2 轮最优权重 ≥ 25；数据 `screen_r2_top.csv`、`screen_r2_top_k24.csv`，每组单线程 CaDiCaL 耗时见文件）。因为 2 轮差分权重再次全部并列，表内次序 = 1 轮雪崩并列打破的次序：

| 名次 | (r0,r1,r2,r3) | 1.5 轮权重 | 2 轮权重下界 | 1 轮雪崩均值 | 1 轮零翻转对 | SIMD 友好 |
|---|---|---|---|---|---|---|
| 1 | (24,21,16,20) | 12 | ≥25 | 0.3814 | 6133 | 是 |
| 2 | (24,21,16,23) | 12 | ≥25 | 0.3814 | 6317 | 是 |
| 3 | (24,21,16,30) | 12 | ≥25 | 0.3813 | 6059 | 是 |
| 4 | (16,21,24,9) | 12 | ≥25 | 0.3812 | 6176 | 是 |
| 5 | (16,21,24,24) | 12 | ≥25 | 0.3812 | 6019 | 是 |
| 6 | (16,21,24,8) | 12 | ≥25 | 0.3812 | 5985 | 是 |
| 7 | (16,5,8,6) | 12 | ≥25 | 0.3800 | 7182 | 是 |
| 8 | (16,21,25,22) | 12 | ≥25 | 0.3792 | 6282 | 否 |
| 9 | (22,17,26,27) | 12 | ≥25 | 0.3790 | 4070 | 否 |
| 10 | (16,25,21,8) | 12 | ≥25 | 0.3787 | 6870 | 否 |
| 11 | (19,17,25,20) | 12 | ≥25 | 0.3784 | 5458 | 否 |
| 12 | (27,17,22,31) | 12 | ≥25 | 0.3783 | 3865 | 否 |
| 13 | (6,14,22,10) | 12 | ≥25 | 0.3777 | 5622 | 否 |
| 14 | (15,23,27,18) | 12 | ≥25 | 0.3776 | 8467 | 否 |
| 15 | (18,24,21,15) | 12 | ≥25 | 0.3772 | 5950 | 否 |
| 16 | (25,17,5,14) | 12 | ≥25 | 0.3771 | 5425 | 否 |
| 17 | (15,10,8,16) | 12 | ≥25 | 0.3771 | 6682 | 否 |
| 18 | (16,10,8,18) | 12 | ≥25 | 0.3771 | 7228 | 是 |
| 19 | (6,17,20,17) | 12 | ≥25 | 0.3770 | 7126 | 否 |
| 20 | (17,7,13,5) | 12 | ≥25 | 0.3768 | 8676 | 否 |
| — | **(16,12,8,7) 基线** | 12 | **≥31**（精确搜索仍在跑） | 0.3558 | 7649 | 是 |

**(16,12,8,7) 的位置**：差分权重上与全部 20 组并列（单 Q 0 / 1 轮 2 / 1.5 轮 12 / 2 轮 ≥25，其中基线因单独跑了更久已证 ≥31，其他组只做到 ≥25，不能据此说基线更好）；扩散速度（1 轮雪崩）上落后，在 1.5 轮最高类的 1338 组里排第 524。要在 2 轮差分上真正分出高下，需要每组小时级的精确搜索（本次单组"≤24?"已要 17～33 分钟）。

**关于 (16,12,8,7) 的位置与建议**：
- 在所有可计算的差分指标上（单 Q、1 轮、1.5 轮）基线都处于最高等价类，没有任何组合在差分权重上严格优于它；能区分的只有扩散速度（雪崩），基线在 1.5 轮最高类里居中（524/1338）。
- 因此**没有证据支持在 v0.2 更换旋转常数**；如果要换，理由只能是 §7.3-4（SIMD 友好），候选如 (24,21,16,20)、(16,21,24,9)（1.5 轮 12、1 轮雪崩最高、r0,r2 都是字节倍数），但它们的 2 轮界见下表，且失去了 ChaCha 十余年的分析积累。**建议维持 (16,12,8,7)。**
- 建议修订 §7.3：第 2 条改为"2 轮（至少 1.5 轮）Fable-P 最优差分特征权重"，并注明单 Q 与 1 轮指标对该结构退化；第 1 条注明 2 轮雪崩对任何常数都饱和，只能用 1 轮或更精细的统计（如 zero_pairs）。

---

## 2026-09-13 — 电脑端阶段 4：轮常量检查（对称固定点 / slide）

**工具**：`tools/check_constants.py`；输出 `data/constants_check.txt`。复现：`python tools/check_constants.py`。

**检查项与结果**：
1. **RC 表**：用 60 位精度 Decimal 重算 frac(ln p_i)·2^32，12 个常量与规范逐一相符；12 个互不相同；不存在 RC[i] = RC[j]<<<16（否则 S[0] 与 S[5] 的注入会在某轮相互抵消关系）；任意两常量最小汉明距离 9；每轮 RC[i] ⊕ (RC[i]<<<16) 非零（对角 Q(0,5,10,15) 内的净常量非零）。
2. **无常量轮函数的不变量（常量必须打破的对象）**：全零状态是 12 轮无常量置换的固定点；"四列相同"子空间（S[4j+k]=S[4j]，2^128 个状态）在无常量轮下 1～12 轮保持不变（列步 4 个 Q 输入相同、对角步同理）；"四行相同"与"16 字全等"在无常量轮下不保持（1 轮即离开）。全一状态在无常量轮 1 轮后仍对称但非固定点。
3. **带常量**：对全零、全一、全 0x80000000、全 1、3 组随机四列相同、3 组随机四行相同、3 组随机全等状态，按 P_12 前缀约定和 fable.py 后缀约定各取 n=1..12 轮，输出**均不对称、均非固定点**（0 例外）。10^4 组随机四列相同状态在 12 轮中每一轮输出均已离开该子空间。
4. **slide**：无相等 RC 对；无 RC[i] = RC[j]<<<k（任意 k）的旋转巧合；RC[6..11]（P_6 所用）不与 RC 的任何其他 6 长窗口相同、自身无周期；相邻 RC 异或差 11 个互不相同（无仿射 slide）。
5. **固定点抽样**：结构化输入对任何 P_n（n=1..12，两种约定）都不是固定点；2^18 组随机状态中 1 轮固定点 0 个。

**判断**：12 轮内不存在由结构导出的对称固定点或 slide 性质；常量注入位置（S[0] 与 S[5] 分属不同列）确实打破了唯一的大不变子空间（四列相同）。说明：这里只能排除结构化固定点，随机置换本身期望约有 1 个固定点，穷举不可行也无意义。另注：`fable.py` 中 r<6 轮的 `permute` 用 RC 后缀，与 P_6 = P_12 后 6 轮的设计一致，两种约定本报告都测了。

---

## 2026-09-13 — 电脑端阶段 5：性能基准（单线程 C，标量 + AVX2 8 路）

**工具**：`c/bench.c`（rdtsc 计时，取 5 次最小值；用相关加法链现场校准核心时钟，把 TSC tick 换算为核心周期；进程绑定 CPU0 + 高优先级）、`c/fable_p_avx2.c`（8 状态并行，每个 __m256i 通道一个状态；<<<16/<<<8 用 vpshufb，<<<12/<<<7 用移位+或）、`c/test_avx2.c`（与标量逐位交叉校验，1～12 轮 × 2000 组 × 8 通道全部一致）。原始输出：`data/bench_result.txt`。

**复现**：
```
clang -O3 -std=c11 -mavx2 c/test_avx2.c c/fable_p.c c/fable_p_avx2.c -o build/test_avx2.exe && ./build/test_avx2.exe
clang -O3 -std=c11 -mavx2 -DFABLE_HAVE_AVX2 c/bench.c c/fable_p.c c/fable_p_avx2.c -o build/bench.exe && ./build/bench.exe 2000000
```

**结果**（Raptor Lake P 核，clang 21 -O3；测试时机器有其他低优先级负载，校准得核心时钟约 3.0 GHz；cpb 按数据阶段每次 P_6 处理 32 字节计）：

| 变体 | 核心周期/次调用 | cpb | ns/次 |
|---|---|---|---|
| 标量 P_6 | 222 | **6.9** | 74 |
| 标量 P_12 | 424 | 13.3 | 141 |
| AVX2 ×8 P_6（含每次转置进出） | 607（8 状态） | 2.4 | 202 |
| AVX2 ×8 P_12（含转置） | 992 | 3.9 | 330 |
| AVX2 ×8 P_6 核心（状态常驻转置布局） | 404 | **1.6** | 134 |
| AVX2 ×8 P_12 核心 | 805 | 3.1 | 268 |

**判断**：标量 P_6 约 37 周期/轮，与 ChaCha 双轮的标量速度同量级；数据阶段 6.9 cpb 对应 §7.1 估计的 18 次运算/字节（约 2.6 运算/周期）。AVX2 8 路核心 1.6 cpb，为标量的 4.4 倍；Fable-Stream 实现应让 8 个分块的状态常驻转置布局（转置进出每次约 200 周期，P_6 场景占 1/3）。ChaCha20 AVX2 约 1.0～1.5 cpb（且每 64 字节只做 10 个双轮，而 Fable 每 32 字节做 6 个双轮，运算量为其 1.2 倍），所以 Fable 数据阶段速度与 ChaCha20 本身同量级，且已含认证（无需再跑 Poly1305 一遍）。注意：核心时钟是负载下现场校准值，绝对 ns 数受当时频率影响，cpb 比值更可靠。NEON 版本本次未做（无 ARM 机器）。
---

## 2026-09-13 — v0.2 阶段 1：速率受限差分搜索（§8-4b）

**决定记录**：v0.2 保留 (16,12,8,7)，算法无改动。2 轮无约束精确搜索于 17:00 终止（累计 9.4 h CPU），最终已证 **2 轮最优特征权重 ≥ 31**（≤30 UNSAT，≤31 未完成），记入 `data/diff/perm_r2_baseline_cadical.log` 末行。

**模型**：`sat/fable_diff.py` 新增 `restrict_rate()` 与命令行开关：
- `--rate-in`：输入差分只允许在 S[0..7]，S[8..15] 输入差分强制为 0（数据阶段攻击者只能通过消息块注入差分）；
- `--cap-out-zero`：输出时 S[8..15] 差分为 0（内部碰撞 = 伪造模型：速率输出差分可被下一块消息抵消，容量必须相同）；
- `--rate-out-nonzero`：输出 S[0..7] 差分非零（密钥流可观测的差分）；
- `--half`：附加一个列步（半轮）。
权重仍是整条轨迹全部模加的 Lipmaa–Moriai 权重（"只统计 S[0..7]"在轨迹模型里没有独立含义：容量字上的代价已在各模加中支付，输出约束只决定哪些轨迹被计入）。

**复现**：
```
python sat/fable_diff.py perm --rounds 0 --half --rot 16,12,8,7 --rate-in
python sat/fable_diff.py perm --rounds 0 --half --rot 16,12,8,7 --rate-in --cap-out-zero
python sat/fable_diff.py perm --rounds 1 --rot 16,12,8,7 --rate-in                     # 696 s
python sat/fable_diff.py perm --rounds 1 --rot 16,12,8,7 --rate-in --rate-out-nonzero  # 963 s
python sat/fable_diff.py perm --rounds 1 --rot 16,12,8,7 --rate-in --cap-out-zero --solver cadical153
python sat/fable_diff.py perm --rounds 2 --rot 16,12,8,7 --rate-in --solver cadical153
python sat/fable_diff.py perm --rounds 2 --rot 16,12,8,7 --rate-in --cap-out-zero --solver cadical153
python sat/verify_trail.py data/diff/perm_r1_rot16_12_8_7_rateI.json --samples 20000000
```
原始数据：`data/diff/perm_r0h_*_rate*.json`、`perm_r1_*_rateI.json`、`perm_r1_*_rateIO.json`、`perm_r1_rateIC0.log`、`perm_r2_rateI.log`、`perm_r2_rateIC0.log`。

**结果**（首次记录 18:18，求解于 23:12 全部终止，表中为最终已证下界；标"运行中"的下界随后可能继续提高，最终值以 log 文件为准）：

| 模型 | 半轮（列步） | 1 轮 | 2 轮 | 3 轮 |
|---|---|---|---|---|
| 无约束（v0.1 阶段 3） | 0（单 Q） | 2（精确） | ≥ 31（已终止） | ≥ 33（拆分界） |
| 速率输入（Δin ⊂ S[0..7]） | 2（精确，0.1 s） | **22（精确，696 s，经验验证）** | **≥ 39**（已终止，k=38 用 5075 s） | ≥ 61（= 22 + 39，拆分界） |
| 速率输入 + 速率输出非零 | — | 22（精确，963 s） | — | — |
| 速率输入 + 容量输出为零（内部碰撞/伪造） | **不可满足**（UNSAT 到 160，0.1 s） | **≥ 43**（已终止，k=42 用 4217 s） | **≥ 41**（已终止，k=40 用 4661 s） | ≥ 63（拆分界） |

1 轮速率受限最优轨迹（权重 22）：Δin = S[0]=0x80088008, S[4]=0x80080008（同一列的 a、b，MSB 派生的 4 比特模式），列步后只剩 S[0]=MSB、S[4]=bit22、S[8]=MSB，对角步付掉大部分代价；输出差分遍布 13 个字。经验验证：2×10^7 组随机状态命中 3 次，期望 2^-22 × 2×10^7 = 4.8 次，泊松统计下一致（脚本按 0.8 倍阈值标 MISMATCH 属于阈值过严）。

**判断**：
- v0.2 §2.4 的推断成立：**概率 1 / 2^-2 的 MSB 轨迹依赖容量字里的差分，数据阶段攻击者拿不到**；把输入差分限制到速率后，1 轮最优特征概率从 2^-2 降到 2^-22，2 轮 ≤ 2^-32（仍在下降）。
- 伪造相关的内部碰撞模型更强：半轮内结构上不可能（列步必把速率差分推入 c、d 两个容量字），1 轮 ≥ 2^-43 且下界还在上升（每级约 1.4 倍耗时），2 轮 ≥ 2^-41。
- 与 P_6 的关系：按拆分界（轨迹可拆成独立段，段与段之间的中间差分不加约束，所以中间段只能用无约束/速率输入界）P_6 的速率受限特征 ≤ 2^-(22 + 31 + 31 + 2) = 2^-86（首段速率输入 1 轮 22、中间两个无约束 2 轮各 ≥ 31、末段无约束 1 轮 2）；内部碰撞模型也只能用同一拆分（末段"容量输出为零"的 36 是在速率输入约束下证的，拆分后末段输入不受限，不能直接套用），同样 ≤ 2^-86；要得到更紧的 P_6 伪造界，需要 3 轮的直接速率受限搜索或"任意输入 + 容量输出为零"的 1 轮界。这是特征（trail）概率的上界，拆分界很松（中间段用了无约束界），离"P_6 内无 > 2^-128 的伪造轨迹"还差 42 bit，要靠 3 轮直接界或更紧的分段界补齐。按 v0.2 §0 的经验余量论证，这一项的结论是：**速率受限模型下没有出现比无约束模型更弱的结构，反而强得多（1 轮 2^-2 → 2^-22，内部碰撞半轮不可能、1 轮 ≤ 2^-43），P_6 的差分伪造余量不是短板。**

---

## 2026-09-13 — v0.2 阶段 2：差分-线性攻击可达轮数评估（§8-4c）

### (a) 文献：ChaCha 上差分-线性 + PNB 攻击的现状（2026-09 检索）

单位说明：下表"轮"均为 **ChaCha 单轮**（列轮或对角轮）；Fable 的 1 轮 = 2 个单轮，P_6 = 12 单轮，P_12 = 24 单轮。ChaCha 攻击模型是 PRF：攻击者选 nonce/计数器、看到完整 512 bit 输出（前馈相加后），密钥 256 bit。

| 单轮数 | 时间 | 数据 | 方法 | 来源 |
|---|---|---|---|---|
| 6 | 2^139 | 2^30 | 差分-线性 + PNB（首次提出） | Aumasson et al. FSE 2008 |
| 6 | 2^77.4 | 2^58 | 首个 3.5 轮单比特区分器 + 划分 | Beierle–Leander–Todo CRYPTO 2020 / JoC 2022 |
| 6 | 2^71.0 | 2^41.6 | PNB + 线性分解 | Dey, IEEE-IT 2024 |
| 6 | **2^57.4** | 2^55.7 | bit puncturing + distillation | Flórez-Gutiérrez–Todo EUROCRYPT 2025 |
| 7 | 2^248 / 2^237.7 / 2^230.86 | — | 早期 PNB 系列 | AFK+08 / CM17 / BLT20 |
| 7 | 2^221.95 / 2^218.92 | 2^90 / 2^87 | 内存/非内存子空间 PNB | Dey et al. EUROCRYPT 2022 / 2023 |
| 7 | 2^210.3 | 2^103.3 | syncopation | Wang et al. CRYPTO 2023 |
| 7 | 2^192.89 | 2^93.79 | PNB + 线性分解 | Dey, IEEE-IT 2024 |
| 7 | 2^189.7 | 2^102.63 | 4 轮区分器的 DL hull（2^−34.15 → 2^−32.2） | Xu et al. ToSC 2024 |
| 7 | 2^178.12 | 2^101.09 | 修正复杂度估计 + 新 PNB 集 | Sharma–Dey–Sarkar–Maitra, ePrint 2025/428 |
| 7 | **2^148.2** | 2^127.7 | bit puncturing | Flórez-Gutiérrez–Todo EUROCRYPT 2025 |
| 7.25 | 2^223.9 → 2^212.43 | ≈2^100.6 | Xu 2024 → ePrint 2025/428 | |
| 7.5 | 2^255.24 | 2^32.64 | 首次 7.5 轮密钥恢复（仅 79% 密钥） | Dey, IEEE-IT 2024 |
| 7.5 | 2^253.23 | 2^34.47 | 改进 PNB | ePrint 2025/428 |
| 7.5 | **2^250.2** | 2^127.1 | bit puncturing（"首个明显优于穷举的 7.5 轮攻击"） | EUROCRYPT 2025 |
| 7.5 | 2^251.4（区分器） | — | divide-and-conquer trail enumeration puncturing | Flórez-Gutiérrez–Todo ASIACRYPT 2025 |

区分器本身：单比特差分-线性区分器最长为 **3.5 单轮**（BLT20，后被 Coutinho 等扩充），4 轮区分器 ε_d = 0.0032 ≈ 2^−8.3（ID X13[6] → OD X2[0]⊕X8[0]⊕X7[7]，3 比特掩码），4.5～5 轮区分器相关性 2^−32.2（Xu 2024 hull 改进 Bellini 2023），再往后靠 PNB/puncturing 反向猜 2.5～3 轮密钥。**20 轮 ChaCha 的余量 = 20/7.5 = 2.7 倍；128 bit 密钥版本止步 6.5 轮左右。** 所有文献一致：8 单轮 ChaCha 目前没有任何优于穷举的攻击。

来源：[ePrint 2025/428](https://eprint.iacr.org/2025/428.pdf)、[ePrint 2025/437（EUROCRYPT 2025）](https://eprint.iacr.org/2025/437)、[Dey IEEE-IT 2024](https://dl.acm.org/doi/abs/10.1109/TIT.2024.3389874)、[Dey et al. EUROCRYPT 2022](https://eprint.iacr.org/2022/536.pdf)、[ASIACRYPT 2025](https://link.springer.com/chapter/10.1007/978-981-95-5018-0_2)。

### (b) Fable-P 单比特差分-线性偏差实测

**工具**：`c/dl_bias.c`（输入单比特差分 e_i，i=0..511；输出对每个比特 j 统计 Pr[输出比特不变]；每个输入比特 2^22 个随机状态；"单轮"= 列步或对角步，常量在每个列步前注入，RC 索引与 `fable_permute` 的后缀约定一致），`tools/dl_report.py`（汇总）。三种变体：`const`（Fable-P 原样）、`noconst`（轮常量置零，即 ChaCha 的轮结构 + 全随机状态）、`chacha`（真 ChaCha 状态布局：S[0..3] 固定为 sigma 常量，密钥/计数器/nonce 随机，差分只放在 S[12..15]，即文献的攻击模型）。原始数据：`data/dl/dl_sr{1..4}_{const,noconst,chacha}.u32`（512×512 uint32 计数）、`.txt`、`summary.md`。

**复现**：
```
clang -O3 -std=c11 c/dl_bias.c c/fable_p.c -o build/dl_bias.exe
for sr in 1 2 3 4; do ./build/dl_bias.exe $sr 4194304 $((100+sr)) data/dl/dl_sr${sr}_const.u32; ./build/dl_bias.exe $sr 4194304 $((100+sr)) data/dl/dl_sr${sr}_noconst.u32 noconst; done
for sr in 3 4; do ./build/dl_bias.exe $sr 4194304 $((200+sr)) data/dl/dl_sr${sr}_chacha.u32 chacha 384 512; done
python tools/dl_report.py
```

**结果**（噪声 σ = 2^-13 ≈ 2.4×10^-4；262144 对的噪声极值期望 ≈ 4.9σ = 1.2×10^-3 ≈ 2^-9.7）：

| 单轮数 | Fable 轮数 | max\|ε\| 全状态（const） | max\|ε\| 速率→速率 | noconst | ChaCha 布局（仅 nonce 字输入） | \|ε\|>2^-8 的对数（const） |
|---|---|---|---|---|---|---|
| 1 | 0.5 | 0.5（确定性） | 0.5 | 0.5 | — | 256104 |
| 2 | 1 | 0.5（确定性） | 0.5 | 0.5 | — | 207934 |
| 3 | 1.5 | 0.1765 = 2^-2.50 | 0.0332 = 2^-4.9 | 2^-2.50 | 0.0991 = 2^-3.34 | 14103 |
| 4 | 2 | 0.00111 = 2^-9.8（= 噪声极值） | 2^-9.9 | 2^-9.8 | 2^-9.8 | **0** |

有/无常量逐对比较：1、2 单轮相关系数 1.0000（常量只在值上平移，差分不变）；3 单轮相关系数 0.9942，有 2473 对差异超过 4σ（第二个常量在第 3 单轮前注入，改变了部分模加的进位分布），但最大偏差 0.1765 vs 0.1762、分布形状不变；4 单轮全部为噪声（相关系数 −0.003）。

**与 ChaCha 文献对比**：3 单轮 ChaCha 单比特差分-线性偏差在文献中约为 2^-3～2^-5 级（Aumasson 等 2008 的 3 轮 ID/OD，Choudhuri–Maitra 2016 的改进），本测量在 ChaCha 布局下最大 2^-3.34（ID S[14].b21 → OD S[0].b0），同量级；4 单轮公开的最好区分器 ε_d = 0.0032 = 2^-8.3 用的是 3 比特输出掩码 + 特定 ID（BLT20），单比特 ID/OD 在随机状态下 4 单轮已低于 2^-9.7 的可测门限，与文献"单比特区分器止于 3.5 轮"一致。Fable 的常量注入**没有改变偏差的分布包络**（最大值、超阈值对数、相关系数都与无常量版本一致），因此 ChaCha 上的差分-线性 / PNB 结果可以直接作为 Fable-P 的度量尺。

### (c) 结论：可达轮数折算与余量

- ChaCha 的轮数换算：Fable 1 轮 = 2 单轮。P_6 = 12 单轮 ≡ ChaCha12；P_12 = 24 单轮 > ChaCha20。
- 最强已知攻击：密钥恢复 **7.5 单轮**（EUROCRYPT 2025，2^250.2），7 单轮 2^148.2；8 单轮无攻击。区分器最长 7.5 单轮（ASIACRYPT 2025，2^251.4，接近穷举）。
- **余量倍数**：P_6：12/7.5 = **1.6×**（相对"首个无攻击轮数" 8：1.5×）；P_12：24/7.5 = **3.2×**（相对 8：3.0×）。ChaCha20 自身为 2.7×，ChaCha12 为 1.6×。
- 模型差异说明（偏保守的方向）：ChaCha 攻击者可选择 128 bit 的 nonce/计数器并看到全部 512 bit 前馈输出；Fable 数据阶段攻击者控制 256 bit 速率差分（更多），但只看到 256 bit 速率输出、且 P_6 之间的状态由 256 bit 未知容量分隔，反向 PNB 阶段要猜的量与 ChaCha 猜密钥相当。初始化 P_12 的模型（密钥 + 可选 nonce → 输出 P_6 后的速率）与 ChaCha PRF 最接近，24 单轮余量充足。
- **判断**：P_12 余量 3.2× 明显满足 §0 的 ≥1.5× 要求。**P_6 = 1.6× 刚好压线**：等价于 ChaCha12 的安全余量（Linux 内核 RNG、Adiantum 等接受这一水平），但 7 轮攻击复杂度 17 年内从 2^248 降到 2^148、7.5 轮在 2024～2025 年成为可行，任何 8 单轮攻击都会让 P_6 跌破 1.5×。建议：v0.2 保持 P_6，但在规范 §0 明确写出"P_6 ≡ ChaCha12 余量（1.6×），P_12 ≡ 3.2×"，并把 §7.2 的"数据阶段降到 5 轮"方向删除（5 轮 = 10 单轮，余量 1.33×，不满足要求）；若希望数据阶段也有 ≥2× 余量，需要 P_8（16 单轮，2.1×，吞吐降 25%）。

---

## 2026-09-13 — v0.2 阶段 3：线性近似（§8-5）

**模型**（`sat/fable_lin.py`）：模加 z = x + y 的线性近似 (u, v → w) 用进位链精确建模（Wallén 2003 / Schulte-Geers 2013）：把 z_i = x_i ⊕ y_i ⊕ c_i、c_{i+1} = maj(x_i, y_i, c_i) 展开，给每个进位比特 c_i 一个掩码变量 g_i；第 i 位的局部近似 a·x_i ⊕ b·y_i ⊕ (g_i⊕w_i)·c_i ⊕ g_{i+1}·maj(x_i,y_i,c_i)（a = u_i⊕w_i，b = v_i⊕w_i）的相关性为：g_{i+1}=0 时要求 a=b=g_i⊕w_i=0（相关性 1）；g_{i+1}=1 时要求 a⊕b⊕(g_i⊕w_i)=1（相关性 ±1/2）；其余为 0。第 0 位无进位输入（g_1=1 恒可，g_1=0 需 a=b=0），g_32=0（进位输出被丢弃）。因为每个状态至多一个后继，进位掩码路径唯一，|corr| = 2^−#{g_i=1} **精确**，不需要求和。XOR：输出掩码复制到两个输入；分叉（一个值被用两次）：掩码异或；旋转：连线置换；轮常量只翻转符号。总权重 W ⇒ 轨迹相关性 2^−W、线性偏差 2^−(W+1)。求解与差分相同（ITotalizer 递增、CaDiCaL）。`sat/verify_lin.py` 经验估计相关性。`--rate-io`：输入掩码与输出掩码都限制在 S[0..7]（密钥流线性区分模型：攻击者只看到速率）。

**复现**：
```
python sat/fable_lin.py q --rot 16,12,8,7
python sat/fable_lin.py perm --rounds 1 --rot 16,12,8,7
python sat/fable_lin.py perm --rounds 1 --rot 16,12,8,7 --rate-io --solver cadical153
python sat/fable_lin.py perm --rounds 2 --rot 16,12,8,7 --solver cadical153
python sat/verify_lin.py data/lin/lin_q_rot16_12_8_7.json
python sat/verify_lin.py data/lin/lin_perm_r1_rot16_12_8_7.json
```
原始数据：`data/lin/*.json`、`lin_r1_rateIO.log`、`lin_r2_all.log`。

**结果**（首次记录 18:18，求解于 23:12 全部终止，表中为最终已证下界；标"运行中"的下界随后可能提高，以 log 为准）：

| 对象 | 最优线性轨迹权重 W | 相关性 | 状态 | 经验验证 |
|---|---|---|---|---|
| 单 Q | 0 | 2^0 | 精确 | 2×10^5 样本：+1.0000 |
| 1 轮 Fable-P | **1** | **2^−1** | 精确（0.2 s） | −0.4976（符号来自常量） |
| 1 轮，速率→速率掩码 | ≥ 28 | ≤ 2^−28 | 已终止（k=27 用 6050 s） | — |
| 2 轮 Fable-P | ≥ 18 | ≤ 2^−18 | 已终止（k=17 用 5605 s） | — |

单 Q 的相关性 1 关系：输入掩码 (a,b,c,d) = (bit0, 0, 0, bit0) → 输出 (0x00010000, 0x00080000, 0x00001001, 0x01000001)，本质是模加最低位 z_0 = x_0 ⊕ y_0 的线性性经四次模加/旋转传递（ChaCha QR 同样存在，是 ARX 的固有性质）。1 轮权重 1 的轨迹：输入掩码 S[0].b0 ⊕ S[4].b0，只在一次模加的第 1 位付 1/2，输出掩码 15 个比特分布在 9 个字。

**判断**：与差分一样，全状态模型下 1 轮线性轨迹相关性极高（2^−1），但 2 轮已 ≤ 2^−18 且下界仍在上升；把掩码限制到攻击者可见的速率后 1 轮就 ≤ 2^−28。线性区分器需要约 corr^−2 个样本，2 轮已 ≥ 2^36、速率受限 1 轮 ≥ 2^56，按 6 轮拆分（rate-1 + 2 + 2 + 1）P_6 的线性轨迹相关性 ≤ 2^−(28+18+18+1) = 2^−65（松界，中间段用无约束界）。线性并不比差分弱，也没有出现比 ChaCha 结构新增的线性关系（常量只影响符号）。未做：线性 hull 效应（多轨迹求和）与 2 轮以上直接界。

---

## 2026-09-13 — v0.2 阶段 4：旋转差分（rotational-XOR）与轮常量（§8-6b）

**工具**：`tools/rx_check.py`；输出 `data/rx_check.txt`。复现：`python tools/rx_check.py`。

**模型**：旋转对 (x, x<<<k) 通过一次模加保持旋转关系的概率 p_k = (1 + 2^(k−32) + 2^−k + 2^−32)/4（Khovratovich–Nikolić 2010），k=1 时 2^−1.415，k=16 时 2^−2.0，这是**每次模加都要付的基础代价，与常量无关**。XOR 常量 c 会把 RX 差分改变 c ⊕ (c<<<k)（Ashur–Liu ToSC 2016）；只有当该值为 0 或权重很低时旋转差分攻击才不受常量阻碍。注入的差分随后要通过模加，代价约为其汉明重量（LSB/MSB 各最多减 1）。

**结果**：

| k | 单次模加 | 每轮（32 加） | P_6 基础代价 | P_12 基础代价 |
|---|---|---|---|---|
| 1 | 2^−1.415 | 2^−45.3 | 2^−271.7 | 2^−543.4 |
| 2 | 2^−1.678 | 2^−53.7 | 2^−322.2 | 2^−644.4 |
| 16 | 2^−2.000 | 2^−64.0 | 2^−384.0 | 2^−768.0 |

常量注入的 RX 差分权重（k=1，每轮 hw(RC⊕RC<<<1) / hw((RC<<<16)⊕(RC<<<17))）：14/14, 18/18, 10/10, 16/16, 16/16, 18/18, 16/16, 10/10, 16/16, 14/14, 16/16, 14/14；对 k=1,2,4,8,16 及全部 k∈1..31 都**没有任何一轮注入差分为 0**，最小值 hw(d0)+hw(d5) = 16（k=4，第 8 轮）。按"注入权重 − 2"的粗下界，常量在 P_6 至少额外贡献 148 bit（k=1）、P_12 至少 308 bit。

合并上界（独立模加假设）：k=1 时 **P_6 的 RX 特征概率 ≤ 2^−419.7，P_12 ≤ 2^−851.4**；k=16 时更低。

经验验证（单 Q，k=1，2^20 样本）：无常量时 Pr[Q(x<<<1) = Q(x)<<<1] = 2^−7.18（独立模型 2^−5.66，链式模加相关性使真实值更低，与 Khovratovich 等 2015 "ARX revisited" 的观察一致）；先向 a 异或 RC[6]（注入 RX 差分 hw=16）后 2^20 样本中 **0 次**命中（< 2^−20）。

**判断**：即使完全不注入常量，Fable-P 的 32 加/轮结构就已使 6 轮的纯旋转概率低于 2^−271，远低于 2^−256；常量注入每轮再叠加 ≥ 20 bit 以上的 RX 代价，且没有任何 k 让某一轮的注入差分为零。**旋转差分攻击对 P_6/P_12 完全不可行，常量足以阻止（其实常量的作用主要在阻止对称/slide 性质，见 v0.1 阶段 4，而不是旋转差分）。** 未做：带常量的 RX 差分 SAT 精确最优（需要 Ashur–Liu 的精确模加 RX 模型），鉴于上述余量没有必要。

---

## 2026-09-13 — v0.2 阶段 5：Fable-Stream 8 路 AVX2 整体实现与吞吐（§8-9b）

**实现**：`c/fable_stream_avx2.c/.h`。8 个满分块并行：16 个 __m256i 寄存器各存 8 个状态的同一个字（通道 = 分块），整个 AEAD（初始化 P_12、AD 吸收 P_6、每 32 字节块 P_6、最终化 P_12）都在转置布局中完成；明文/密文与状态之间用 8×8 32-bit 转置（unpack/permute2x128，每块两次）交换。不足 8 个的剩余分块和末块（含空末块、非 32 倍数分块尾部）走标量 `fable_aead_encrypt`。输出格式与 `fable.py` 的 `stream_encrypt` 完全一致。`fable_stream_encrypt_scalar` 为纯标量对照。

**正确性**：`tests/check_stream_avx2.py` 与 Python 参考逐字节比对 9 种组合（分块 32/1000/4096/65536 字节，长度 0 ～ 590 KB，覆盖 1～3 个 x8 组、剩余分块、空末块、非 32 倍数分块），AVX2 与标量输出均与 Python 一致，且 Python `stream_decrypt` 能解密 AVX2 输出。`RESULT: ALL OK`。

**复现**：
```
bash c/build.sh && python tests/check_stream_avx2.py
clang -O3 -std=c11 -mavx2 c/bench_stream.c c/fable_stream_avx2.c c/fable_p_avx2.c c/fable_p.c c/fable_aead.c -o build/bench_stream.exe && ./build/bench_stream.exe 1024 65536 3
openssl speed -evp chacha20-poly1305 -seconds 2
python tools/bench_chacha.py 1024
```
原始输出：`data/bench_stream_result.txt`、`data/bench_openssl_speed.txt`、`data/bench_chacha_result.txt`。

**吞吐**（单线程，1 GiB 输入，64 KiB 分块，Raptor Lake P 核，测试时机器另有低优先级后台负载；核心时钟按相关加法链现场校准为 3.65 GHz）：

| 实现 | 时间 | GB/s | cpb |
|---|---|---|---|
| **Fable-Stream AVX2 ×8** | 0.545 s | **1.97** | **1.85** |
| Fable-Stream 标量 | 2.434 s | 0.44 | 8.27 |
| ChaCha20-Poly1305，OpenSSL 3.5（python cryptography 48，1 次调用 1 GiB） | 0.706 s | 1.52 | ≈2.4 |
| ChaCha20-Poly1305，OpenSSL 3.5，64 KiB 分块循环 | 0.739 s | 1.45 | ≈2.5 |
| ChaCha20-Poly1305，libsodium（pynacl 1.6.2 内置库，1 次调用） | 2.791 s | 0.39 | — |
| OpenSSL 3.5.4 `speed -evp`（Git 自带 mingw 版，16 KiB 块） | — | 1.17 | — |

**判断**：Fable-Stream 的 8 路 AVX2 实现单线程 1.97 GB/s、1.85 cpb，**比同机 OpenSSL 的 ChaCha20-Poly1305 快约 30%**，且已含认证；与 v0.1 阶段 5 的置换核心 1.6 cpb 相比，转置、AD、初始化/最终化（每 64 KiB 分块两次 P_12 + 一次 P_6，约 0.2%）和标量末块的开销合计约 15%。规范 §7.1"与 ChaCha20-Poly1305 同量级（±30%）"的预期成立，且偏向有利的一侧。pynacl 自带的 libsodium 明显没有启用 AVX2 路径（0.39 GB/s），不作为参考。标量 8.3 cpb 高于阶段 5 的置换 6.9 cpb，差额来自每块的字节加载/存储和 AEAD 逻辑。多线程未测（分块独立，理论线性扩展）。

---

## 2026-09-14 — v0.3 阶段 1：参数集切换（§8-9d）

**变更**：`c/fable_aead.h/.c` 新增 `FABLE_SET_DEFAULT`（P_12/P_8，IV1 = 0x012020C8）与 `FABLE_SET_F`（P_12/P_6，IV1 = 0x012020C6），`fable_aead_encrypt_set / fable_aead_decrypt_set` 带参数集参数，原 `fable_aead_encrypt/decrypt` 成为默认集包装；`fable_set_light_rounds()`、`fable_set_iv1()` 暴露参数集常量。`c/fable_stream_avx2.h/.c` 同样加 `_set` 版本（`fable_stream_encrypt_{x8,scalar}_set`），并新增解密 `fable_stream_decrypt_{x8,scalar}_set`（8 路 AVX2 解密：密文块转置后作为新的速率，明文 = 旧速率 ⊕ 密文；尾块用密钥流异或后再以 10* 填充回注；8 个标签逐通道恒定时间比较，任一失败即清零输出并返回 −1；头部/截断/缺末块也返回 −1）。置换、常量、模式代码无改动。

**校验**（`tests/check_c_vectors.py`、`tests/check_stream_avx2.py`，均改为两参数集都跑）：
```
bash c/build.sh && python tests/check_c_vectors.py && python tests/check_stream_avx2.py
```
- `permute12_zero`、`permute8_zero` 与 `test_vectors_v0.3.json` 一致。
- 两参数集各 5 组 AEAD 向量：C 加密 = 向量 = Python；解密还原；篡改检测；**Fable-f 的 5 组向量与 v0.1 向量逐字节相同**（`test_vectors_v0.1.json` 内容 = v0.3 的 `fable-f` 节）。
- 默认包装 = SET_DEFAULT；hash256 两组向量一致；1～12 轮置换与随机旋转量交叉校验一致；两参数集各 36 组随机长度 AEAD 与 Python 一致。
- Stream：两参数集 × 9 种分块/长度组合，AVX2 与标量加密 = Python；AVX2 与标量解密还原明文；Python 能解 C 输出；中间分块翻 1 bit、末标签翻 1 bit、截断 40 字节、用另一参数集解密，全部正确拒绝。`RESULT: ALL OK` × 2。

**判断**：参数集切换在 C 端与 Python 端语义一致，两参数集通过 IV1 域分离（用错参数集解密必失败），Fable-f 与 v0.1 完全兼容。

---

## 2026-09-14 — v0.3 阶段 2：默认参数集吞吐复测 + 解密 AVX2 + 多线程（§8-9b/9c）

**工具**：`c/bench_stream.c`（重写：两参数集 × 加密/解密 × 标量/AVX2 ×8 × 1/4/8 线程；主线程固定 CPU0，工作线程逐个固定到不同 P 核逻辑 0,2,…,14；多线程用 `fable_stream_encrypt_range_x8 / decrypt_range_x8` 按分块区间切分，末尾不足 8 的分块与末块由主线程标量处理；核心时钟用相关加法链现场校准）。对照：`tools/bench_chacha.py`、`openssl speed`。原始输出：`data/bench_stream_v03.txt`、`data/bench_chacha_v03.txt`、`data/bench_openssl_speed_v03.txt`。

**复现**：
```
clang -O3 -std=c11 -mavx2 c/bench_stream.c c/fable_stream_avx2.c c/fable_p_avx2.c c/fable_p.c c/fable_aead.c -o build/bench_stream.exe
./build/bench_stream.exe 1024 65536 3 1,4,8
python tools/bench_chacha.py 1024 ; openssl speed -evp chacha20-poly1305 -seconds 2
```

**结果**（1 GiB，64 KiB 分块，Raptor Lake，核心时钟校准 4.89 GHz，机器另有 3 个 BelowNormal 单线程求解在跑）：

| 参数集 | 操作 | 路径 | 线程 | GB/s | cpb | 相对单线程 |
|---|---|---|---|---|---|---|
| **Fable (P_8)** | 加密 | 标量 | 1 | 0.48 | 10.2 | — |
| Fable | 解密 | 标量 | 1 | 0.51 | 9.6 | — |
| **Fable** | **加密** | **AVX2 ×8** | **1** | **2.00** | **2.44** | 1× |
| Fable | 解密 | AVX2 ×8 | 1 | 2.05 | 2.39 | — |
| Fable | 加密 | AVX2 ×8 | 4 | 6.54 | — | 3.3× |
| Fable | 解密 | AVX2 ×8 | 4 | 6.31 | — | 3.1× |
| Fable | 加密 | AVX2 ×8 | 8 | 11.5 | — | 5.7× |
| Fable | 解密 | AVX2 ×8 | 8 | 10.2 | — | 5.0× |
| Fable-f (P_6) | 加密 | 标量 | 1 | 0.62 | 7.9 | — |
| **Fable-f** | **加密** | **AVX2 ×8** | **1** | **2.53** | **1.93** | 1× |
| Fable-f | 解密 | AVX2 ×8 | 1 | 2.59 | 1.89 | — |
| Fable-f | 加密 | AVX2 ×8 | 4 | 8.24 | — | 3.3× |
| Fable-f | 加密 | AVX2 ×8 | 8 | 14.5 | — | 5.7× |
| ChaCha20-Poly1305，OpenSSL 3.5（cryptography 48，1 次调用 1 GiB，未绑核） | 加密 | — | 1 | 1.81 | — | — |
| 同上，64 KiB 分块循环 | 加密 | — | 1 | 2.27 | — | — |
| OpenSSL 3.5.4 `speed -evp` 16 KiB 块 | — | — | 1 | 1.81 | — | — |
| libsodium（pynacl 内置，无 AVX2 路径） | 加密 | — | 1 | 0.69 | — | — |

**判断**：
- **默认参数集 Fable (P_8) 单线程 AVX2 ×8：2.00 GB/s、2.44 cpb**，与同机 OpenSSL ChaCha20-Poly1305（1.8～2.3 GB/s）**持平**，符合 §7.1 "按运算量线性外推约 1.5 GB/s、与 ChaCha20-Poly1305 持平"的预期（实际略好于外推）。Fable-f 2.53 GB/s、1.93 cpb，比 OpenSSL 快 10%～40%。P_8 相对 P_6 的代价：吞吐 −21%（理论运算量 +33%，因初始化/最终化 P_12 和转置开销被摊薄，实际差距小于理论值）。
- v0.2 阶段 5 的 fable-f 1.97 GB/s 是在核心时钟 3.65 GHz（重负载）下测得，cpb 1.85 与本次 1.93 一致；本次绝对值更高是频率差异。第一次复测时因主线程未绑核被调度到 E 核，数字低一半（已修正为固定 P 核）。
- **解密 AVX2** 与加密同速（2.39 vs 2.44 cpb），标签逐通道恒定时间比较，1 GiB 明文逐字节验证。
- **多线程扩展**：4 线程 3.1～3.3×，8 线程 5.0～5.7×（8 个 P 核全占，HT 未用，E 核未用）。亚线性主要来自全核负载时的睿频下降与内存带宽（8 线程 11.5 GB/s 读 + 11.5 GB/s 写已接近双通道 DDR 的实际可用带宽的一半），不是算法瓶颈；分块独立，理论上线性。
- 与 §0 参数集表一致：默认集用 2.1× 余量换取约 20% 吞吐，仍不慢于 ChaCha20-Poly1305。

---

## 2026-09-14 — v0.3 阶段 5：密钥承诺归约（§8-7）

**输出**：`docs/key-commitment.md`。

**归约结构**：记最终化前的完整状态 X = (Y_R, Y_C ⊕ K)，T = trunc_τ(P_12(X))。任何 CMT-4（因此也是 CMT-1）攻击者给出两组不同输入产生相同 (C, T)，必落入三种事件之一：(a) X ≠ X'，则得到 F(x) = trunc_τ(P_12(x)) 的碰撞——生日界 q²/2^{τ+1}；(b) X = X' 且 K ≠ K'，则两条 duplex 路径的最终状态满足固定的 512 bit 关系（速率相等、容量差 = K ⊕ K'）——q²/2^{513}；(c) 猜中容量——q/2^{256}。理想置换模型下 Adv ≤ q²/2^{τ+1} + q²/2^{513} + q/2^{256}。

**结论**：承诺强度 = τ/2 bit，**256 bit 标签 → 128 bit，128 bit 标签 → 64 bit**，与规范 §0 一致且是紧的（通用生日攻击达到）。"标签不与密钥异或"不改变强度但让归约只依赖 P_12 截断像的碰撞难度；两参数集的 IV1 域分离不影响承诺；承诺是离线碰撞问题，与用户数、nonce 无关；理想模型的实际前提是 P_12（24 单轮）无碰撞捷径，由差分界（2 轮 ≥ 31 ⇒ 12 轮拆分 ≤ 2^-186）与 ChaCha 分析轮数（≤ 7.5）支撑。建议：需要承诺的场景用 ≥ 192 bit 标签。

---

## 2026-09-14 — v0.3 阶段 6：多用户与 STREAM 组合安全界（§8-8）

**输出**：`docs/security-bounds.md`。

**界的形式**（DMV17 多用户 keyed duplex 界按 Fable 参数写出，精细项 L、Ω 按最坏情况合并进 M，HRRV15 的 STREAM 归约无损）：

  Adv ≤ [A] μN/2^k + [B] MN/2^c + [C] M²/2^{c+1} + [D] (M+N)²/2^{b+1} + [E] q_D/2^τ + [F] F²/2^{153}

| 项 | 决定参数 | μ = 2^32、每用户 2^64 字节（M ≈ 2^91）、N = 2^128、q_D = 2^64 |
|---|---|---|
| [A] | k = 256 | 2^-96 |
| [B] | **c = 256** | **2^-37**（单用户 2^-69） |
| [C] | c = 256 | 2^-75（单用户 2^-139） |
| [D] | b = 512 | 2^-256 |
| [E] | τ | 2^-192（τ=256）/ 2^-64（τ=128） |
| [F] | **n_f = 152** | 2^-89（2^32 文件）～ **2^-57**（2^48 个单分块文件） |

**结论**：(1) c = 256 让 keyed duplex 达到 JLM14 的 min{2^{b/2}, 2^c, 2^k} 级安全，单用户 2^64 字节 + 2^128 离线计算仍有 2^-69 余量；(2) 多用户退化由 [A]、[B] 主导，恰为 log2(μ) bit，符合 §0；(3) **最大的实际风险项是随机文件 nonce 的碰撞 [F]**（152 bit，2^-57～2^-89），建议规范 §4.1 注明并提供计数器式 N_f 选项；(4) §7.2 的速率 320（c = 192）会让多用户界 [B] 在 M = 2^91、N = 2^128 时超过 1，只能作为单用户限量变体；(5) STREAM 归约把 Fable-Stream 的 nOAE 安全无损地归结为按分块计数的 AEAD 界。P_8/P_6/P_12 的轮数不出现在模式层的界中，它们决定理想置换假设是否成立（由差分/线性/差分-线性余量支撑）。

---

## 2026-09-14 — v0.3 阶段 3：收紧 P_8 伪造界（§8-4b'）

**模型**：同 v0.2 阶段 1（`sat/fable_diff.py`，Lipmaa–Moriai 精确模加差分，CaDiCaL 递增求解），新增 `--kstart`（从已证下界起步）。

**复现**：
```
python sat/fable_diff.py perm --rounds 3 --rot 16,12,8,7 --rate-in --kstart 41 --solver cadical153     # (a)
python sat/fable_diff.py perm --rounds 1 --rot 16,12,8,7 --cap-out-zero --solver cadical153            # (b)
python sat/verify_trail.py data/diff/perm_r1_rot16_12_8_7_rateC0.json --samples 1000000
```
原始数据：`data/diff/perm_r3_rateI.log`、`perm_r1_rot16_12_8_7_rateC0.json`、`perm_r1_C0.log`。

### (a) 速率输入约束下的 3 轮直接下界

起点 41 = 2 轮速率输入下界 39 + 1 轮无约束 2（拆分界，无需求解）。SAT 从 k=41 开始逐级证明，每级 4 小时上限。结果：02:05 启动，k≤41 UNSAT 用 3596 s，k≤42 UNSAT 用 4706 s，k≤43 未完成即到 4 小时上限（06:05）终止。**3 轮速率输入最优权重 ≥ 43**（比拆分界 41 高 2），每级耗时约 ×1.3 增长。

### (b) "任意输入差分 + 容量输出差分为零"的 1 轮最优权重（末段模型）

**精确值 6**（1.1 s）。轨迹：输入差分分布在 12 个字（0x80000000 / 0x80008000 / 0x80808080 等 MSB 派生模式），列步后只剩 S[3]、S[9]、S[14] 三个字有差分，对角步把它们全部收进 S[3]（速率）并让 8 个容量字差分归零；经验验证 10^6 组：命中率 1.541×10^-2 ≈ 2^-6.02（预测 2^-6）。对比：速率输入 + 容量输出为零的 1 轮 ≥ 43，说明"容量输出为零"本身很便宜（MSB 技巧），贵的是"输入只能在速率"。

### (c) P_8 的最紧拆分界

拆分原理：任意 R 轮轨迹按轮切成连续段，段间中间差分不加约束，因此总权重 ≥ 各段在**只保留该段自身边界约束**下的最优权重之和。可用的段界（全部已证，"≥"为 SAT 下界，其余为精确值）：

| 段 | 约束 | 权重 |
|---|---|---|
| 1 轮 | 速率输入 | 22（精确） |
| 2 轮 | 速率输入 | ≥ 39 |
| 3 轮 | 速率输入 | ≥ 43（SAT，4 h 上限时终止） |
| 1 轮 | 无约束 | 2（精确） |
| 2 轮 | 无约束 | ≥ 31 |
| 1 轮 | 任意输入 + 容量输出为零 | 6（精确） |
| 1 轮 | 速率输入 + 容量输出为零 | ≥ 43 |
| 2 轮 | 速率输入 + 容量输出为零 | ≥ 41 |

伪造（内部碰撞）模型 = 首段速率输入 + 末段容量输出为零。因为去掉约束只会让段界变小，末段可以直接用无约束界；候选拆分：

| 拆分（轮数：所用界） | 权重和 |
|---|---|
| **2（速率输入 39）+ 2（无约束 31）+ 2（无约束 31）+ 2（无约束 31）** | **132** |
| 2（39）+ 2（31）+ 2（31）+ 1（无约束 2）+ 1（容量输出零 6） | 109 |
| 3（速率输入 43）+ 2（31）+ 2（31）+ 1（2） | 107 |
| 1（速率输入 22）+ 2（31）+ 2（31）+ 2（31）+ 1（2） | 117 |

**结论：P_8 在数据阶段伪造模型下的差分特征概率 ≤ 2^-132**（首段 2 轮速率输入 ≥ 39，其余三个 2 轮无约束段各 ≥ 31），**首次达到 2^-128 目标**。同一方法：Fable-f 的 P_6 ≤ 2^-(39+31+31) = 2^-101（v0.2 时的 2^-86 因当时 2 轮速率输入只证到 32）。密钥流可观测模型（速率输入 + 速率输出非零）与速率输入相同，界也是 2^-132。

**说明**：
- 决定性的是两个 2 轮界（速率输入 ≥ 39、无约束 ≥ 31）；(a) 的 3 轮直接界要超过 39 + 31 = 70 才能改善拆分，SAT 不可能在合理时间内证到，(a) 的价值在于确认 3 轮速率输入下界随轮数的增长趋势。(b) 的 6 说明"末段容量输出为零"不是可利用的约束，末段直接用无约束界即可。
- 这是特征（trail）概率界，差分（differential，多条特征聚合）可能更高；但 2^-132 到 2^-128 之间只有 4 bit 余量，若要抵御聚合效应需要继续推高 2 轮界（每 +1 权重约需 1.5～2 倍求解时间，当前单级 1～2 小时）。按 v0.3 §0 的经验余量论证，这一项已不再是短板。

---

## 2026-09-14 — v0.3 阶段 4：线性 hull（§8-5b）

**工具**：`sat/lin_hull.py`。`enumerate`：固定输入与输出掩码为给定轨迹的掩码，用 SAT 枚举权重 ≤ wmax 的**全部**线性轨迹（对全部内部连线掩码变量加阻塞子句，逐条枚举），每条轨迹的符号由 maj/AND 的 Walsh 系数（单变量 +1/2、三变量 −1/2；第 0 位 AND 与 x⊕y 相关为 −1/2）和轮常量（(−1)^{⟨m_S0, RC⟩ ⊕ ⟨m_S5, RC<<<16⟩}）精确计算，求和得 hull 相关性；1 轮可与经验相关性对照。`find --fix-in/--fix-mid/--fix-out`：固定一个掩码求 2 轮轨迹（上界）。

**复现**：
```
python sat/lin_hull.py enumerate --trail data/lin/lin_perm_r1_rot16_12_8_7.json --wmax 6 --samples 200000
python sat/lin_hull.py find --rounds 2 --fix-in data/lin/lin_perm_r1_rot16_12_8_7.json --kmax 80
python sat/lin_hull.py enumerate --trail data/lin/lin_perm_r2_fixin.json --wmax 54   # 58, 60 同理
python sat/fable_lin.py perm --rounds 2 --rot 16,12,8,7 --kstart 18 --solver cadical153   # 2 轮最优继续
```
原始数据：`data/lin/hull_r1_w6.json`、`hull_r2_w54.json`、`hull_r2_w58.json`、`hull_r2_w60.json`、`lin_perm_r2_fixin.json`、`lin_r2_fix{mid,out}.log`、`lin_r2_all_cont.log`。

**1 轮**（最优轨迹：输入 S[0].b0 ⊕ S[4].b0，权重 1）：权重 ≤ 6 的轨迹**只有 1 条**（就是它本身），hull = 单轨迹 = −2^-1；经验相关性 −0.5017（2×10^5 样本，σ = 2.2×10^-3）。符号（含常量翻转）预测正确，**1 轮无 hull 效应**。

**2 轮**：
- 无约束最优：v0.2 已证 ≥ 18，本次从 18 续证，k≤18 UNSAT 用 7184 s，**≥ 19**（k≤19 的证明未完成即于 06:06 截止终止，最终已证下界 19；见 `lin_r2_all_cont.log`）。因此**权重 ≤ 24 的枚举目前没有任何已知的 2 轮掩码对可用**（若最终最优 > 24，则对任意掩码对 "≤ 24 的 hull" 为空集）。
- 用 1 轮最优结构延拓找 2 轮轨迹：固定输入 = 1 轮最优输入 → 最小权重 **48**；固定中间掩码 = 1 轮最优输入（让第 2 轮走权重 1）→ ≥ 62（已停）；固定输出 = 1 轮最优输出 → ≥ 55（已停）。说明 1 轮最优（LSB 线性关系）的结构不能延拓，2 轮最优（19～48 之间）是另一种结构。
- **hull 枚举**（掩码对 = 固定输入得到的权重 48 轨迹的输入/输出）：

| wmax | 轨迹数 | 按权重分布 | 正/负号 | hull 和 | 相对单轨迹 2^-48 |
|---|---|---|---|---|---|
| 54 | 544 | 48:1, 50:13, 52:90, 53:1, 54:439 | 181 / 363 | −2^-45.02 | ×7.9 |
| 58 | 7812 | … 56:1693, 58:5498 | 3470 / 4342 | −2^-44.78 | ×9.3 |
| 60 | 23745 | … 59:289, 60:15644 | 11374 / 12371 | −2^-44.78 | ×9.3（已收敛） |

**判断**：2 轮的 hull 效应**显著**：同一掩码对下有上万条低权重轨迹，符号相关（负号占优），叠加后相关性比最优单轨迹高约 **2^3.2（9 倍）**，且在 wmax = 58 已收敛（≥ 60 的轨迹贡献 < 1%）。含义：(1) 用单轨迹权重换算的线性相关性上界在 2 轮以上要预留约 3 bit 的 hull 余量；(2) 由于 2 轮最优单轨迹 ≥ 19，即使叠加 3 bit，2 轮 hull 相关性也 ≤ 2^-16，6 轮/8 轮按拆分（1 + 2 + 2 + 1 / 2 + 2 + 2 + 2）仍远低于可利用水平（P_8：4 × 19 − 4 × 3 ≈ 2^-64 的保守估计，数据需求 2^128）；(3) 1 轮的 LSB 关系是孤立的（无 hull），与 ChaCha 的已知性质一致。

---

## 2026-09-14 — v0.4 后台长跑（§8-4b''、5c）与论文起草

**规范**：`Fable-spec-v0.4.md`（纯文档修订，算法无改动）已提交。

**后台长跑**（BelowNormal 优先级，48 小时上限，08:20 启动；每证一级更新下表并提交）：
```
python sat/fable_diff.py perm --rounds 2 --rot 16,12,8,7 --rate-in --kstart 39 --solver cadical153 --out data/diff/perm_r2_rateI_v04.json
python sat/fable_diff.py perm --rounds 2 --rot 16,12,8,7 --kstart 31 --solver cadical153 --out data/diff/perm_r2_unc_v04.json
python sat/fable_lin.py  perm --rounds 2 --rot 16,12,8,7 --kstart 19 --solver cadical153 --out data/lin/lin_r2_v04.json
```
日志：`data/diff/perm_r2_rateI_v04.log`、`data/diff/perm_r2_unc_v04.log`、`data/lin/lin_r2_v04.log`。目标：两个差分界各 +4（39→43、31→35），使 P_8 伪造拆分界从 2^-132 升到 2^-(43+3×35) = 2^-148（覆盖聚合效应）。

| 时间 | 2 轮速率输入（差分） | 2 轮无约束（差分） | 2 轮线性 | P_8 拆分界 |
|---|---|---|---|---|
| 08:20 起点 | ≥ 39 | ≥ 31 | ≥ 19 | 2^-132 |
| 10:20（k≤39 UNSAT，7172 s） | **≥ 40** | ≥ 31 | ≥ 19 | 2^-133 |
| 11:36（无约束 k≤31 UNSAT，11731 s） | ≥ 40 | **≥ 32** | ≥ 19 | 2^-136 |

**论文**：`paper/fable.tex`（IACR ToSC `iacrtrans` 类）、`paper/refs.bib`。逐节起草，每节经作者审阅后再写下一节；所有数字取自本日志。

---

## 2026-09-14 — v0.4 补充：绑核复测 ChaCha20-Poly1305 与 AES-256-GCM 对照

**方法**：`tools/bench_chacha.py`（改为用 psutil 把进程绑到逻辑 CPU0 并设 HIGH 优先级，与 `c/bench_stream.c` 条件一致）、`tools/bench_openssl_pinned.py`（以同样绑核/优先级启动 `openssl speed -evp chacha20-poly1305` 与 `-evp aes-256-gcm`，3 秒/块长）。同机、同会话（三个 BelowNormal 求解仍在后台）。原始输出：`data/bench_chacha_v04_pinned.txt`、`data/bench_openssl_speed_v04_pinned.txt`。

**复现**：
```
python tools/bench_chacha.py 1024
python tools/bench_openssl_pinned.py 3
```

| 实现 | GB/s（绑核） | v0.3 阶段 2 未绑核值 |
|---|---|---|
| ChaCha20-Poly1305，OpenSSL 3.5（cryptography 48），1 次调用 1 GiB | **1.57** | 1.81 |
| ChaCha20-Poly1305，OpenSSL 3.5，64 KiB 分块循环 | **1.94** | 2.27 |
| ChaCha20-Poly1305，OpenSSL 3.5.4 `speed -evp` 16 KiB | **2.37** | 1.81 |
| **AES-256-GCM**，OpenSSL 3.5.4 `speed -evp` 16 KiB（AES-NI + PCLMULQDQ） | **6.07** | — |
| ChaCha20-Poly1305，libsodium（pynacl 内置，无 AVX2 路径） | 0.75 | 0.69 |
| Fable AVX2 ×8（v0.3 阶段 2） | 2.00 | — |
| Fable-f AVX2 ×8 | 2.53 | — |

**判断**：绑核后 OpenSSL 的三个数字为 1.57～2.37 GB/s（Python 驱动的两项略降、`speed` 略升），Fable 2.00 GB/s 仍在该区间内，"与 ChaCha20-Poly1305 持平"的结论不变；Fable-f 2.53 比其中最高值快 7%、比 1 次调用快 60%。AES-256-GCM 在 AES-NI 上 6.07 GB/s，是两者的 3 倍：Fable 的定位是 ChaCha20 的生态位（无 AES 加速平台、零成本恒定时间、长 nonce 与承诺），有 AES-NI 且不需要这些性质时 AES-GCM/AEGIS 更快。论文第 5 节对照表与结论已按此更新。
