# Fable 认证加密算法 —— 设计规范 v0.1（草案）

**状态：研究草案。未经公开密码分析，禁止用于任何真实数据。**

版本：0.1　日期：2026-09-13
本规范定义三部分：置换 **Fable-P**、认证加密模式 **Fable-AEAD**、大文件分块模式 **Fable-Stream**，并附带一个哈希/XOF 模式 **Fable-Hash**。

---

## 0. 目标与安全声明

| 目标 | 参数 / 声明 |
|---|---|
| 密钥长度 | 256 bit |
| Nonce 长度 | 192 bit（可安全随机生成） |
| 标签长度 | 256 bit（默认）；允许截断，最短 128 bit |
| 机密性 / 完整性（经典） | 128 bit（数据量 ≤ 2^64 字节/密钥） |
| 抗量子密钥恢复 | Grover 后 ≥ 128 bit（256 bit 密钥） |
| 密钥承诺（key commitment） | 128 bit（256 bit 标签时）；128 bit 标签时降为 64 bit |
| Nonce 误用 | 默认模式**不**声明抗误用；抗误用变体 Fable-SIV 留待 v0.2 |
| 多用户 | 声明目标：μ 个用户下安全性退化 ≤ log2(μ) bit |
| 实现安全 | 无查表、无数据相关分支，天然恒定时间 |
| 目标平台 | 通用 CPU（x86 AVX2 / ARM NEON 优先），无需专用指令 |

**设计原则**：新设计集中在置换 Fable-P 一处；模式层全部采用已有安全证明的构造（keyed duplex、STREAM、CTX 式承诺），使风险集中在一个可分析的对象上。

---

## 1. 记号

- 所有字为 32 bit 无符号整数，小端序。
- `+`：模 2^32 加法；`⊕`：异或；`<<< n`：32 bit 循环左移。
- 状态 `S` 为 16 个字 `S[0..15]`，按 4×4 矩阵排布（行优先）：

```
S[ 0] S[ 1] S[ 2] S[ 3]
S[ 4] S[ 5] S[ 6] S[ 7]
S[ 8] S[ 9] S[10] S[11]
S[12] S[13] S[14] S[15]
```

- 速率部分 `R = S[0..7]`（256 bit），容量部分 `C = S[8..15]`（256 bit）。
- `P_n` 表示 Fable-P 迭代 n 轮。

---

## 2. 置换 Fable-P

### 2.1 总体结构

Fable-P 作用于 512 bit 状态，每轮由三步组成：

1. **常量注入**（ι）
2. **列混合**（column step）
3. **对角混合**（diagonal step）

```
Round_i(S):
    S[0]  ⊕= RC[i]
    S[5]  ⊕= RC[i] <<< 16
    Q(S[0], S[4], S[ 8], S[12]);  Q(S[1], S[5], S[ 9], S[13])
    Q(S[2], S[6], S[10], S[14]);  Q(S[3], S[7], S[11], S[15])
    Q(S[0], S[5], S[10], S[15]);  Q(S[1], S[6], S[11], S[12])
    Q(S[2], S[7], S[ 8], S[13]);  Q(S[3], S[4], S[ 9], S[14])
```

轮数：
- `P_12`：初始化与最终化（重轮）
- `P_6`：数据吸收与挤出（轻轮）

### 2.2 四分之一轮 Q

```
Q(a, b, c, d):
    a += b;  d ⊕= a;  d <<<= r0
    c += d;  b ⊕= c;  b <<<= r1
    a += b;  d ⊕= a;  d <<<= r2
    c += d;  b ⊕= c;  b <<<= r3
```

**旋转常数（临时）**：`(r0, r1, r2, r3) = (16, 12, 8, 7)`。
这组常数是 32 bit ARX 中被分析最充分的一组，作为 v0.1 的基线。§8 中的分析计划要求对全部 32^4 组合做筛选，筛选标准见 §7.3；若发现更优组合则在 v0.2 替换。

### 2.3 常量注入

常量注入是 Fable-P 区别于 ChaCha 核心的关键部分：ChaCha 作为 PRF 使用时依赖输入常量，而作为海绵置换使用时必须由轮常量打破轮间对称与滑动性质（否则存在平凡的对称固定点和 slide 攻击）。

常量注入到两个字（`S[0]` 与 `S[5]`），分别位于不同列、不同行、且同属第一条对角线，使常量在一轮内同时进入两个独立的列混合和同一个对角混合。

**轮常量 RC[i]**：取自然对数 ln(p_i) 的小数部分前 32 bit，p_i 为第 i+1 个素数。选择 ln 而非平方根/立方根是为了与 SHA-2 的常量来源区分，避免任何"复用常量"的误解；这是标准的 nothing-up-my-sleeve 做法。

| i | p | RC[i] | i | p | RC[i] |
|---|---|---|---|---|---|
| 0 | 2 | `0xb17217f7` | 6 | 17 | `0xd54d783f` |
| 1 | 3 | `0x193ea7aa` | 7 | 19 | `0xf1c6c0c0` |
| 2 | 5 | `0x9c041f7e` | 8 | 23 | `0x22afbfba` |
| 3 | 7 | `0xf2272ae3` | 9 | 29 | `0x5e071979` |
| 4 | 11 | `0x65dc76ef` | 10 | 31 | `0x6f19c912` |
| 5 | 13 | `0x90a08566` | 11 | 37 | `0x9c651dc7` |

`P_6` 使用 `RC[6..11]`（即轻轮使用重轮的后 6 个常量，与 Ascon 做法一致，保证轻轮是重轮的后缀，便于统一分析）。

### 2.4 设计理由摘要

- **512 bit / 32 bit 字**：一个状态恰好填满一条 AVX2 寄存器组的 1/2，或一条 NEON 四寄存器组；Fable-Stream 的分块彼此独立，可让 4～8 个状态在 SIMD 通道上并行。
- **列/对角交替**：每个字在一轮后影响全部 16 个字（完整扩散在 1 轮内达成，逐比特雪崩在约 2 轮达到 50%）。
- **ARX 而非 S 盒**：无查表，恒定时间零成本；每字节数据成本约 12 次 ARX 运算（见 §7.1）。

---

## 3. Fable-AEAD（单文件 / 单消息）

Keyed duplex 构造，参照 Ascon / NORX 的证明框架（Jovanovic–Luykx–Mennink 2014）。

### 3.1 参数

- `K`：256 bit（8 字）
- `N`：192 bit（6 字）
- `A`：关联数据，任意长度
- `M`：明文，任意长度（单消息上限 2^64 字节；大文件应使用 Fable-Stream）
- `T`：256 bit 标签

### 3.2 初始化

```
IV0 = 0x4661626C            # ASCII "Fabl"
IV1 = 0x012020C6            # 版本 0x01 | 密钥字节 0x20 | 速率字节 0x20 | 轮数 (12<<4 | 6)

S = IV0 || IV1 || K[0..7] || N[0..5]        # 2 + 8 + 6 = 16 字
S = P_12(S)
S[8..15] ⊕= K[0..7]                          # 密钥再注入容量，防止状态恢复直接导致密钥恢复
```

> 注：状态布局中 `IV0, IV1` 位于 `S[0], S[1]`，密钥位于 `S[2..9]`，nonce 位于 `S[10..15]`。IV1 编码全部参数，保证不同参数集之间域分离。

### 3.3 关联数据吸收

将 `A` 按 256 bit 分块，末块做 `10*` 填充（若 `A` 为空则不做任何吸收）：

```
for each block A_j:
    S[0..7] ⊕= A_j
    S = P_6(S)
S[15] ⊕= 0x80000000        # 域分离：AD 结束
```

### 3.4 加密

将 `M` 按 256 bit 分块，末块做 `10*` 填充：

```
for each full block M_j:
    S[0..7] ⊕= M_j
    C_j = S[0..7]
    S = P_6(S)
# 末块（可能不满）：
S[0..7] ⊕= pad(M_last)
C_last = S[0..7] 截断为 |M_last| bit
```

### 3.5 最终化与标签

```
S[8..15] ⊕= K[0..7]
S = P_12(S)
T = S[0..7]                 # 256 bit
```

标签直接取速率部分，而**不**再与密钥异或。理由（密钥承诺）：标签是状态经 `P_12` 后的输出，攻击者要为两个不同密钥构造相同标签，需要在最终置换前找到导致相同 256 bit 输出的两个状态，这归约为对置换输出的碰撞搜索，代价约 2^128（256 bit 标签）。密钥在最终化前已被注入容量部分，因此标签隐式承诺了密钥。此声明需在 §8 中通过形式化归约验证。

### 3.6 解密

对称过程；标签比较必须恒定时间；验证失败时不得输出任何明文。

---

## 4. Fable-Stream（大文件）

采用 STREAM 构造（Hoang–Reyhanitabar–Rogaway–Vizár, CRYPTO 2015），提供**在线、可并行、抗截断、抗重排**的 nonce-based AE，已有安全证明。

### 4.1 参数

- 块大小 `L`：默认 64 KiB（65536 字节），可配置，须在文件头记录。
- 文件 nonce `N_f`：152 bit，随机生成。
- 每块 nonce：`N_i = N_f || ctr_i(32 bit) || flag(8 bit)`，共 192 bit。
  - `ctr_i` 从 0 开始递增，最多 2^32 块（64 KiB 块时上限 256 TiB）。
  - `flag = 0x00` 表示中间块，`0x01` 表示最后一块。

### 4.2 结构

```
文件头 = magic "FBLS" || 版本(1) || L(4) || N_f(19 字节)
块 i   = Fable-AEAD(K, N_i, A = 文件头, M = 明文块 i)  →  C_i || T_i
```

- 每块产生一个 256 bit 标签，64 KiB 块的开销为 0.05%。
- 最后一块用 `flag=0x01`，即使明文长度恰为 `L` 的整数倍也必须有一个（可能为空的）末块，防止截断攻击。
- 解密时任一块验证失败立即中止，已输出的前序明文块视为已验证（这是 STREAM 的在线安全性质）。

### 4.3 并行性

块间完全独立，可用线程或 SIMD 通道并行；单线程 SIMD 实现应同时处理 4（AVX2）或 8（AVX-512）个块。

---

## 5. Fable-Hash / Fable-XOF

标准海绵，`r = 256, c = 256`，全部使用 `P_12`：

```
S = 0x4661626C || 0x01202000 || 0^14 字      # IV1 的轮数字段置 0 以区分 AEAD
吸收：每 256 bit 块异或到 S[0..7] 后 P_12，末块 10* 填充
挤出：输出 S[0..7]，需要更多则 P_12 后继续
```

安全声明：碰撞 128 bit，原像 256 bit（XOF 输出 ≥ 512 bit 时）。

---

## 6. 明确不声明的内容

- 不声明 nonce 重复时的任何安全性（重复 nonce 会泄露明文异或与认证能力）。
- 不声明对故障注入、探针类物理攻击的安全性。
- 不声明发布前的任何安全性——本文档在被独立分析之前只是一个假设集合。

---

## 7. 性能模型

### 7.1 运算计数

- 一个 Q = 4 加 + 4 异或 + 4 旋转 = 12 次运算；一轮 = 8 Q = 96 次运算。
- 数据阶段每 32 字节调用 `P_6` = 576 次运算 → **18 次/字节**（含认证，单遍）。
- 对比：ChaCha20 每字节 10 次运算 + Poly1305 约 1 次乘法/16 字节 + 额外一遍内存访问。预计 Fable 单遍数据阶段在纯软件平台与 ChaCha20-Poly1305 处于同一量级（±30%），并行度更高时可反超；实测待 C/SIMD 版本。

### 7.2 可能的加速方向（v0.2 评估）

- 速率提升至 320 bit（`c = 192`）：keyed duplex 在 2^64 数据量下仍可维持约 128 bit 安全，吞吐提升 25%。
- 数据阶段轮数降至 5：需先完成 §8 的差分/线性界。

### 7.3 旋转常数筛选标准（供 §8 使用）

1. 2 轮后逐比特雪崩偏差最小（每输入比特翻转后输出每比特翻转概率与 0.5 的最大偏差）。
2. 1 轮 Q 的最优差分特征概率上界最低（枚举或 SAT）。
3. 4 轮内无概率 > 2^-128 的差分/线性路径（MILP/SAT 搜索）。
4. 同等条件下优先选择编译器能映射为单条指令的旋转量（如 8、16 在部分 SIMD 上是字节重排）。

---

## 8. 分析计划（发布前必须完成）

| # | 项目 | 工具 | 状态 |
|---|---|---|---|
| 1 | 参考实现 + 测试向量 | Python | 待做 |
| 2 | 雪崩与扩散测试（1～4 轮） | Python / NumPy | 待做 |
| 3 | 旋转常数全空间筛选 | Python + SAT | 待做 |
| 4 | 差分特征概率下界（≤ 6 轮） | MILP (Gurobi) / CryptoSMT | 待做（需电脑） |
| 5 | 线性近似偏差下界 | 同上 | 待做（需电脑） |
| 6 | 差分-线性、旋转、slide、对称固定点 | 手工 + 脚本 | 待做 |
| 7 | 密钥承诺归约形式化 | 手工 | 待做 |
| 8 | 多用户与 STREAM 组合证明检查 | 手工 | 待做 |
| 9 | C + AVX2/NEON 实现与吞吐测试 | C | 待做（需电脑） |
| 10 | 撰写论文并公开 | — | 待做 |

---

## 9. 版本记录

- **v0.1**（2026-09-13）：首版。定义置换、AEAD、Stream、Hash；旋转常数与轮数标记为临时。
