# Fable 认证加密算法 —— 设计规范 v0.3（草案）

**状态：研究草案。未经公开密码分析，禁止用于任何真实数据。**

版本：0.3　日期：2026-09-13
本规范定义三部分：置换 **Fable-P**、认证加密模式 **Fable-AEAD**、大文件分块模式 **Fable-Stream**，并附带一个哈希/XOF 模式 **Fable-Hash**。

---

## 0. 目标与安全声明

| 目标 | 参数 / 声明 |
|---|---|
| 密钥长度 | 256 bit |
| Nonce 长度 | 192 bit（可安全随机生成） |
| 标签长度 | 256 bit（默认）；允许截断，最短 128 bit |
| 机密性 / 完整性（经典） | 128 bit（数据量 ≤ 2^64 字节/密钥） |
| 抗量子密钥恢复 | Grover 后 ≥ 128 bit（256 bit 密钥） |
| 密钥承诺（key commitment） | 128 bit（256 bit 标签时）；128 bit 标签时降为 64 bit |
| Nonce 误用 | 默认模式**不**声明抗误用；抗误用变体 Fable-SIV 留待 v0.2 |
| 多用户 | 声明目标：μ 个用户下安全性退化 ≤ log2(μ) bit |
| 实现安全 | 无查表、无数据相关分支，天然恒定时间 |
| 目标平台 | 通用 CPU（x86 AVX2 / ARM NEON 优先），无需专用指令 |

**设计原则**：新设计集中在置换 Fable-P 一处；模式层全部采用已有安全证明的构造（keyed duplex、STREAM、CTX 式承诺），使风险集中在一个可分析的对象上。

**参数集（v0.3）**：

| 参数集 | 初始化/最终化 | 数据阶段 | IV1 | 数据阶段经验余量 | 定位 |
|---|---|---|---|---|---|
| **Fable**（默认） | P_12 | **P_8** | `0x012020C8` | 16 单轮 / 7.5 = **2.1×** | 通用、推荐 |
| Fable-f | P_12 | P_6 | `0x012020C6` | 12 单轮 / 7.5 = 1.6×（≡ ChaCha12） | 吞吐优先，接受 ChaCha12 级余量的场景 |

两个参数集共用置换、常量、模式与密钥；IV1 的轮数字节保证两者域分离，同一密钥下的输出互不相关。初始化 P_12 余量 24 / 7.5 = 3.2×，两者相同。

**安全论证方式（v0.2 明确）**：Fable-P 是 ARX 置换，与 ChaCha/Salsa 同类。ARX 结构不存在 SPN 式的可证明差分/线性活跃度下界——单个 Q 函数存在概率 1 的差分（§2.5），多轮特征概率的 SAT 精确求解在 2 轮以上不可行。因此 Fable-P 的安全声明采用**经验安全余量**论证：以最强已知攻击族（差分-线性 + 概率中性比特、旋转差分、代数/立方）能攻破的轮数为基准，要求默认参数集的数据阶段余量 ≥ 2 倍、初始化阶段 ≥ 1.5 倍（Fable-f 放宽至数据阶段 ≥ 1.5 倍）。以 2026-09 的文献（最强密钥恢复 7.5 单轮，EUROCRYPT 2025）为基准，Fable 数据阶段 2.1×、Fable-f 1.6×、初始化 3.2×。这与 ChaCha20 和 Ascon 的实际安全论证方式一致，规范不再声称"4 轮内无 > 2^-128 特征"。

---

## 1. 记号

- 所有字为 32 bit 无符号整数，小端序。
- `+`：模 2^32 加法；`⊕`：异或；`<<< n`：32 bit 循环左移。
- 状态 `S` 为 16 个字 `S[0..15]`，按 4×4 矩阵排布（行优先）：

```
S[ 0] S[ 1] S[ 2] S[ 3]
S[ 4] S[ 5] S[ 6] S[ 7]
S[ 8] S[ 9] S[10] S[11]
S[12] S[13] S[14] S[15]
```

- 速率部分 `R = S[0..7]`（256 bit），容量部分 `C = S[8..15]`（256 bit）。
- `P_n` 表示 Fable-P 迭代 n 轮。

---

## 2. 置换 Fable-P

### 2.1 总体结构

Fable-P 作用于 512 bit 状态，每轮由三步组成：

1. **常量注入**（ι）
2. **列混合**（column step）
3. **对角混合**（diagonal step）

```
Round_i(S):
    S[0]  ⊕= RC[i]
    S[5]  ⊕= RC[i] <<< 16
    Q(S[0], S[4], S[ 8], S[12]);  Q(S[1], S[5], S[ 9], S[13])
    Q(S[2], S[6], S[10], S[14]);  Q(S[3], S[7], S[11], S[15])
    Q(S[0], S[5], S[10], S[15]);  Q(S[1], S[6], S[11], S[12])
    Q(S[2], S[7], S[ 8], S[13]);  Q(S[3], S[4], S[ 9], S[14])
```

轮数：
- `P_12`：初始化与最终化（重轮）
- `P_8`：数据吸收与挤出（轻轮，默认参数集）
- `P_6`：数据吸收与挤出（Fable-f 参数集）

### 2.2 四分之一轮 Q

```
Q(a, b, c, d):
    a += b;  d ⊕= a;  d <<<= r0
    c += d;  b ⊕= c;  b <<<= r1
    a += b;  d ⊕= a;  d <<<= r2
    c += d;  b ⊕= c;  b <<<= r3
```

**旋转常数（v0.2 确认）**：`(r0, r1, r2, r3) = (16, 12, 8, 7)`。
v0.2 筛选结论（详见 ANALYSIS-LOG 阶段 3）：在单 Q、1 轮、1.5 轮差分特征权重上，全部 32^4 组合（或其代表样本）与基线处于同一等价类，无任何组合在差分指标上严格优于基线；2 轮权重 21 组候选均 ≥ 25，基线已证 ≥ 31。可区分的只有 1 轮扩散速度，基线居中。由于差分指标无法给出更换理由，且该组常数拥有 ChaCha 十余年的分析积累，**确定保留**。`16` 与 `8` 在 AVX2/NEON 上可用字节重排实现。

### 2.3 常量注入

常量注入是 Fable-P 区别于 ChaCha 核心的关键部分：ChaCha 作为 PRF 使用时依赖输入常量，而作为海绵置换使用时必须由轮常量打破轮间对称与滑动性质（否则存在平凡的对称固定点和 slide 攻击）。

常量注入到两个字（`S[0]` 与 `S[5]`），分别位于不同列、不同行、且同属第一条对角线，使常量在一轮内同时进入两个独立的列混合和同一个对角混合。

**轮常量 RC[i]**：取自然对数 ln(p_i) 的小数部分前 32 bit，p_i 为第 i+1 个素数。选择 ln 而非平方根/立方根是为了与 SHA-2 的常量来源区分，避免任何"复用常量"的误解；这是标准的 nothing-up-my-sleeve 做法。

| i | p | RC[i] | i | p | RC[i] |
|---|---|---|---|---|---|
| 0 | 2 | `0xb17217f7` | 6 | 17 | `0xd54d783f` |
| 1 | 3 | `0x193ea7aa` | 7 | 19 | `0xf1c6c0c0` |
| 2 | 5 | `0x9c041f7e` | 8 | 23 | `0x22afbfba` |
| 3 | 7 | `0xf2272ae3` | 9 | 29 | `0x5e071979` |
| 4 | 11 | `0x65dc76ef` | 10 | 31 | `0x6f19c912` |
| 5 | 13 | `0x90a08566` | 11 | 37 | `0x9c651dc7` |

`P_n`（n < 12）使用 `RC[12−n..11]`：`P_8` 用 `RC[4..11]`，`P_6` 用 `RC[6..11]`。轻轮是重轮的后缀，与 Ascon 做法一致，便于统一分析。

### 2.4 已知结构性质（v0.2 记录）

- **单 Q 概率 1 差分**：Δ(a,b,c,d) = (0, MSB, 0, MSB⊕bit(31−r0)) 以概率 1 通过一个 Q（差分只在最高位行走，四次模加不付进位代价）。这是 ChaCha QR 的已知性质，对任意旋转常数都成立。Fable-P 1 轮最优特征权重 2，2 轮 ≥ 31。
- **对 keyed duplex 的意义**：该轨迹要求 Δd 非零，而在列步 `Q(S[j], S[4+j], S[8+j], S[12+j])` 中 `d = S[12+j]` 属于容量部分，攻击者在数据阶段只能控制速率 `S[0..7]`。因此 §8 要求补做**速率受限差分搜索**（输入差分限制在 S[0..7]，输出只观测 S[0..7]），这才是伪造攻击对应的正确模型。
- **无常量下的不变子空间**：四列相同子空间（2^128 个状态）在无常量轮下保持不变；常量注入 S[0] 与 S[5]（不同列）已验证在每一轮都破坏该子空间（ANALYSIS-LOG 阶段 4）。

### 2.5 设计理由摘要

- **512 bit / 32 bit 字**：一个状态恰好填满一条 AVX2 寄存器组的 1/2，或一条 NEON 四寄存器组；Fable-Stream 的分块彼此独立，可让 4～8 个状态在 SIMD 通道上并行。
- **列/对角交替**：每个字在一轮后影响全部 16 个字（完整扩散在 1 轮内达成，逐比特雪崩在约 2 轮达到 50%）。
- **ARX 而非 S 盒**：无查表，恒定时间零成本；每字节数据成本约 12 次 ARX 运算（见 §7.1）。

---

## 3. Fable-AEAD（单文件 / 单消息）

Keyed duplex 构造，参照 Ascon / NORX 的证明框架（Jovanovic–Luykx–Mennink 2014）。

### 3.1 参数

- `K`：256 bit（8 字）
- `N`：192 bit（6 字）
- `A`：关联数据，任意长度
- `M`：明文，任意长度（单消息上限 2^64 字节；大文件应使用 Fable-Stream）
- `T`：256 bit 标签

### 3.2 初始化

```
IV0 = 0x4661626C            # ASCII "Fabl"
IV1 = 0x012020C8            # 版本 0x01 | 密钥字节 0x20 | 速率字节 0x20 | 轮数 (12<<4 | 8)
                            # Fable-f：0x012020C6（轮数 12<<4 | 6）

S = IV0 || IV1 || K[0..7] || N[0..5]        # 2 + 8 + 6 = 16 字
S = P_12(S)
S[8..15] ⊕= K[0..7]                          # 密钥再注入容量，防止状态恢复直接导致密钥恢复
```

> 注：状态布局中 `IV0, IV1` 位于 `S[0], S[1]`，密钥位于 `S[2..9]`，nonce 位于 `S[10..15]`。IV1 编码全部参数，保证不同参数集之间域分离。

### 3.3 关联数据吸收

将 `A` 按 256 bit 分块，末块做 `10*` 填充（若 `A` 为空则不做任何吸收）：

```
for each block A_j:
    S[0..7] ⊕= A_j
    S = P_d(S)                  # P_d = P_8（默认）或 P_6（Fable-f）
S[15] ⊕= 0x80000000        # 域分离：AD 结束
```

### 3.4 加密

将 `M` 按 256 bit 分块，末块做 `10*` 填充：

```
for each full block M_j:
    S[0..7] ⊕= M_j
    C_j = S[0..7]
    S = P_d(S)
# 末块（可能不满）：
S[0..7] ⊕= pad(M_last)
C_last = S[0..7] 截断为 |M_last| bit
```

### 3.5 最终化与标签

```
S[8..15] ⊕= K[0..7]
S = P_12(S)
T = S[0..7]                 # 256 bit
```

标签直接取速率部分，而**不**再与密钥异或。理由（密钥承诺）：标签是状态经 `P_12` 后的输出，攻击者要为两个不同密钥构造相同标签，需要在最终置换前找到导致相同 256 bit 输出的两个状态，这归约为对置换输出的碰撞搜索，代价约 2^128（256 bit 标签）。密钥在最终化前已被注入容量部分，因此标签隐式承诺了密钥。此声明需在 §8 中通过形式化归约验证。

### 3.6 解密

对称过程；标签比较必须恒定时间；验证失败时不得输出任何明文。

---

## 4. Fable-Stream（大文件）

采用 STREAM 构造（Hoang–Reyhanitabar–Rogaway–Vizár, CRYPTO 2015），提供**在线、可并行、抗截断、抗重排**的 nonce-based AE，已有安全证明。

### 4.1 参数

- 块大小 `L`：默认 64 KiB（65536 字节），可配置，须在文件头记录。
- 文件 nonce `N_f`：152 bit，随机生成。
- 每块 nonce：`N_i = N_f || ctr_i(32 bit) || flag(8 bit)`，共 192 bit。
  - `ctr_i` 从 0 开始递增，最多 2^32 块（64 KiB 块时上限 256 TiB）。
  - `flag = 0x00` 表示中间块，`0x01` 表示最后一块。

### 4.2 结构

```
文件头 = magic "FBLS" || 版本(1) || L(4) || N_f(19 字节)
块 i   = Fable-AEAD(K, N_i, A = 文件头, M = 明文块 i)  →  C_i || T_i
```

- 每块产生一个 256 bit 标签，64 KiB 块的开销为 0.05%。
- 最后一块用 `flag=0x01`，即使明文长度恰为 `L` 的整数倍也必须有一个（可能为空的）末块，防止截断攻击。
- 解密时任一块验证失败立即中止，已输出的前序明文块视为已验证（这是 STREAM 的在线安全性质）。

### 4.3 并行性

块间完全独立，可用线程或 SIMD 通道并行；单线程 SIMD 实现应同时处理 4（AVX2）或 8（AVX-512）个块。

---

## 5. Fable-Hash / Fable-XOF

标准海绵，`r = 256, c = 256`，全部使用 `P_12`：

```
S = 0x4661626C || 0x01202000 || 0^14 字      # IV1 的轮数字段置 0 以区分 AEAD
吸收：每 256 bit 块异或到 S[0..7] 后 P_12，末块 10* 填充
挤出：输出 S[0..7]，需要更多则 P_12 后继续
```

安全声明：碰撞 128 bit，原像 256 bit（XOF 输出 ≥ 512 bit 时）。

---

## 6. 明确不声明的内容

- 不声明 nonce 重复时的任何安全性（重复 nonce 会泄露明文异或与认证能力）。
- 不声明对故障注入、探针类物理攻击的安全性。
- 不声明发布前的任何安全性——本文档在被独立分析之前只是一个假设集合。

---

## 7. 性能模型

### 7.1 运算计数

- 一个 Q = 4 加 + 4 异或 + 4 旋转 = 12 次运算；一轮 = 8 Q = 96 次运算。
- 数据阶段每 32 字节调用 `P_8` = 768 次运算 → **24 次/字节**（含认证，单遍）；Fable-f 为 18 次/字节。
- 实测（v0.2 阶段 5，Raptor Lake，AVX2 ×8，1 GiB，64 KiB 分块）：Fable-f 1.97 GB/s、1.85 cpb，比同机 OpenSSL 3.5 的 ChaCha20-Poly1305（1.52 GB/s）快约 30%。按运算量线性外推，默认参数集约 1.5 GB/s，与 ChaCha20-Poly1305 持平；实测待 v0.3 复测。

### 7.2 可能的加速方向（v0.4 评估）

- 速率提升至 320 bit（`c = 192`，Ascon-128a 式声明）：吞吐提升 25%，但安全声明需改为依赖数据量上限，且速率受限差分分析与 Stream 实现须全部重做。留待默认参数集分析闭环后评估。
- ~~数据阶段轮数降至 5~~：v0.2 差分-线性评估后删除（10 单轮余量 1.33×，不满足要求）。

### 7.3 旋转常数筛选标准（v0.2 修订）

v0.1 的四条标准中，第 1 条在 2 轮对所有常数饱和（偏差被噪声淹没），第 2 条对本结构全部为 0，第 3 条在 ARX 上不可判定。修订为：

1. **2 轮 Fable-P 最优差分特征权重**（SAT，Lipmaa–Moriai 模型），至少给出 1.5 轮精确值与 2 轮下界。
2. **速率受限 2 轮特征权重**（输入差分 ⊂ S[0..7]，输出观测 S[0..7]）。
3. **1 轮雪崩统计**：平均翻转率与零翻转对数（2 轮已饱和，不再使用）。
4. 同等条件下优先编译器能映射为字节重排的旋转量（8、16、24）。

以上标准已用于 v0.2 的确认，结论是保留 (16,12,8,7)。

---

## 8. 分析计划（发布前必须完成）

| # | 项目 | 工具 | 状态 |
|---|---|---|---|
| 1 | 参考实现 + 测试向量 | Python / C | **完成**（Python 与 C 逐位一致） |
| 2 | 雪崩与扩散测试（1～6 轮，5×10^5 样本） | C | **完成**：2 轮完整雪崩，无 0.005 级偏差 |
| 3 | 旋转常数筛选 | SAT | **完成**：保留 (16,12,8,7) |
| 4a | 差分特征权重（1～2 轮精确/下界） | SAT | **完成**：1 轮 = 2；2 轮 ≥ 31 |
| 4b | 速率受限差分搜索 | SAT | **完成**：速率输入 1 轮 = 22、2 轮 ≥ 38；内部碰撞半轮不可能、1 轮 ≥ 43 |
| 4b' | 速率受限 3 轮直接界；"任意输入 + 容量输出为零" 1 轮界（收紧 P_8 伪造拆分界） | SAT | 待做 |
| 4c | 差分-线性攻击可达轮数评估 | 文献 + 脚本 | **完成**：最强攻击 7.5 单轮；常量不改变偏差包络 |
| 5a | 线性近似偏差（1～2 轮） | SAT | **完成**：1 轮 = 2^-1；速率→速率 1 轮 ≤ 2^-28；2 轮 ≤ 2^-18 |
| 5b | 线性 hull 与 2 轮以上直接界 | SAT | 待做 |
| 6a | slide、对称固定点、常量检查 | 脚本 | **完成**：无结构化固定点/slide |
| 6b | 旋转差分（rotational-XOR，含常量） | 脚本 | **完成**：P_6 ≤ 2^-419，不可行 |
| 7 | 密钥承诺归约形式化（§3.5 声明） | 手工 | 待做 |
| 8 | 多用户与 STREAM 组合证明检查 | 手工 | 待做 |
| 9a | C 标量 + AVX2 ×8 置换与基准 | C | **完成**：标量 6.9 cpb，AVX2 核心 1.6 cpb |
| 9b | Fable-Stream 8 路 AVX2 整体实现 + 吞吐 | C | **完成**（Fable-f）：1.97 GB/s；默认参数集待复测 |
| 9c | NEON 版本；解密端 AVX2；多线程 | C | 待做 |
| 9d | 参数集切换（默认 P_8 / Fable-f P_6）+ 新测试向量 | Python / C | 待做（本版本首要项） |
| 10 | 撰写论文并公开 | — | 待做 |

---

## 9. 版本记录

- **v0.1**（2026-09-13）：首版。定义置换、AEAD、Stream、Hash；旋转常数与轮数标记为临时。
- **v0.3**（2026-09-13）：默认数据阶段由 P_6 改为 **P_8**（余量 1.6× → 2.1×），原 12/6 保留为 **Fable-f** 参数集；IV1 默认改为 `0x012020C8`；§0 写明两参数集余量；§7.2 删除 5 轮方向；§8 全面更新。**v0.1 测试向量仅对 Fable-f 继续有效**，默认参数集需新向量。
- **v0.2**（2026-09-13）：旋转常数 (16,12,8,7) 由临时改为确认；§0 明确采用经验安全余量论证，撤回"4 轮内无 > 2^-128 特征"目标；新增 §2.4 记录单 Q 概率 1 差分与速率受限模型；§7.3 筛选标准全面修订；§8 更新状态并新增速率受限搜索、差分-线性评估、旋转差分、Stream 8 路实现。算法本身（置换、模式、常量、轮数）**无任何改动**，v0.1 测试向量继续有效。
